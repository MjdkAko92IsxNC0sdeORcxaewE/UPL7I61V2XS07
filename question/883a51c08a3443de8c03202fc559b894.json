[
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: onlyRole modifier / EXECUTOR_ROLE] Can an unprivileged attacker, under the precondition that EXECUTOR_ROLE has been granted to address(0) via AccessControl.grantRole (making hasRole(EXECUTOR_ROLE, address(0)) == true and thus onlyRole(EXECUTOR_ROLE) passes for any caller), call execute() on a previously scheduled and ready SystemState.setMCR(lower_value) operation, bypassing the intended privileged-executor gate, violating the invariant that only authorized executors may trigger parameter changes that affect liquidation eligibility and reward distribution, causing scoped impact: attacker pre-deposits into StabilityPool, then executes the MCR-lowering operation themselves, immediately liquidates newly undercollateralized troves, and claims excess BNB and SABLE gains from StabilityPool that they would not have been entitled to under the original MCR? Local proof idea: deploy TimeLock with EXECUTOR_ROLE granted to address(0); schedule setMCR(1.001e18) with minDelay; warp past delay; call execute() from an unprivileged EOA; assert MCR changed; deposit into SP; liquidate a trove that was safe under old MCR; assert BNBGain > 0 for attacker.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: onlyRole modifier / PROPOSER_ROLE] Can an unprivileged attacker, under the precondition that PROPOSER_ROLE has been granted to address(0) (hasRole(PROPOSER_ROLE, address(0)) == true), call schedule() to queue a SystemState.setBorrowingFeeFloor(0) operation, wait minDelay, then (if EXECUTOR_ROLE is also open or held) execute it, violating the invariant that borrowing fee revenue must flow to SableStakingV2 stakers, causing scoped impact: after fee floor is zeroed, all new USDS borrowing generates zero fees sent to SableStakingV2.increaseF_USDS, so existing stakers receive zero USDS reward accumulation while the attacker (who unstaked before the change) already claimed their full entitlement, constituting unfair reward access? Local proof idea: grant PROPOSER_ROLE to address(0); call schedule(SystemState, 0, abi.encodeCall(setMCR, 0), 0, salt, minDelay) from attacker EOA; warp; execute; assert borrowingFeeFloor == 0; open trove; assert no USDS fee sent to staking; assert F_USDS unchanged.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> updateDelay(0) -> schedule() + execute() same block] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE first schedule and execute a self-referential updateDelay(0) call (target = TimeLock, data = abi.encodeCall(updateDelay, 0)), then after _minDelay becomes 0, schedule and immediately execute SystemState.setRedemptionFeeFloor(0) in the same block, bypassing the intended governance delay window that allows users to exit before parameter changes, violating the invariant that all SystemState mutations must be preceded by a non-zero delay, causing scoped impact: attacker redeems USDS for BNB with zero redemption fee, extracting BNB collateral value that should have been partially distributed as fees to SableStakingV2 stakers? Local proof idea: schedule updateDelay(0) with current minDelay; warp; execute; assert _minDelay == 0; schedule setRedemptionFeeFloor(0) with delay=0; execute in same block; assert redemptionFeeFloor == 0; redeem USDS; assert zero fee sent to staking.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: schedule() with arbitrary salt / replay] Can an attacker with PROPOSER_ROLE schedule the same SystemState parameter-change operation multiple times using different salt values (each producing a distinct id via hashOperation), executing each after minDelay, effectively replaying the same governance action repeatedly without any additional authorization, violating the invariant that each distinct governance decision requires a fresh authorization cycle, causing scoped impact: attacker repeatedly lowers MCR across multiple scheduled operations, each time liquidating newly eligible troves and claiming StabilityPool BNB gains, accumulating excess reward value beyond a single authorized parameter change? Local proof idea: schedule setMCR(X) with salt=1; execute after delay; schedule setMCR(X-epsilon) with salt=2; execute after delay; assert MCR decremented twice; assert attacker BNBGain from SP increased proportionally.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: executeBatch() multi-step atomic] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE schedule and execute a batch operation (executeBatch) that atomically: (1) calls SystemState.setMCR(lower) to make troves liquidatable, (2) calls an attacker-controlled contract that liquidates those troves via TroveManager, (3) calls StabilityPool to claim BNB gains, all within a single transaction, bypassing the user-exit window that the timelock delay is supposed to provide, violating the invariant that parameter changes and their downstream reward consequences must be separated by the minDelay, causing scoped impact: attacker atomically extracts BNB liquidation gains from StabilityPool in the same transaction as the MCR change, with no opportunity for other depositors to exit? Local proof idea: construct batch with [setMCR, liquidate, withdrawFromSP]; schedule as batch; warp past delay; executeBatch; assert attacker received BNB gain in same tx.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _call() external call with value / BNB extraction] Can an attacker with EXECUTOR_ROLE schedule and execute an operation where target is an attacker-controlled contract and value equals the TimeLock's BNB balance (accumulated via receive()), draining BNB held by the TimeLock that was intended for protocol maintenance or future operations, violating the invariant that BNB held by the TimeLock must only be used for authorized protocol operations, causing scoped impact: attacker extracts BNB from TimeLock balance to their own address, constituting unauthorized fund extraction? Local proof idea: send BNB to TimeLock via receive(); schedule call to attacker EOA with value=TimeLock.balance; warp; execute; assert attacker ETH balance increased by TimeLock.balance.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: schedule() + on-chain event front-running / StabilityPool pre-positioning] Can an unprivileged StabilityPool depositor observe the CallScheduled event for a pending setMCR(lower_value) operation and deposit a large USDS amount into StabilityPool before the operation executes (during the minDelay window), then after execution liquidate newly undercollateralized troves to claim BNB gains proportional to their inflated deposit, violating the invariant that StabilityPool reward entitlement should not be manipulable by front-running scheduled governance changes, causing scoped impact: attacker claims disproportionate BNB liquidation gains relative to depositors who did not observe or act on the pending governance change? Local proof idea: emit CallScheduled for setMCR; in same block, call provideToSP with large amount; warp past delay; execute setMCR; liquidate trove; assert attacker BNBGain > fair_share based on pre-event deposit ratio.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> CommunityIssuance.updateRewardPerSec] Can an attacker with EXECUTOR_ROLE (assuming TimeLock is owner of CommunityIssuance) schedule and execute a call to CommunityIssuance.updateRewardPerSec(very_high_value), observe the pending CallScheduled event, deposit into StabilityPool just before execution to capture the elevated SABLE issuance rate from the first second, then withdraw after claiming excess SABLE gains, violating the invariant that SABLE reward rate changes must not allow pre-positioned depositors to claim more SABLE than depositors who were present before the rate change, causing scoped impact: attacker claims excess SABLE rewards from CommunityIssuance by front-running the rate increase? Local proof idea: schedule updateRewardPerSec(1e18); deposit into SP; warp to just before delay expires; execute; warp 1 block; withdrawFromSP; assert SABLE gained > proportional share of pre-existing depositors.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> CommunityIssuance.updateRewardPerSec(0)] Can an attacker with EXECUTOR_ROLE schedule and execute CommunityIssuance.updateRewardPerSec(0) to halt SABLE issuance, having first withdrawn their own StabilityPool deposit and claimed all accumulated SABLE gains, while other depositors who remain in the pool receive zero future SABLE rewards, violating the invariant that all depositors with equal stake duration and size must receive equal SABLE reward entitlement, causing scoped impact: attacker extracts their full SABLE reward entitlement while permanently denying future SABLE rewards to remaining depositors? Local proof idea: deposit equal amounts for attacker and victim; warp to accumulate SABLE; attacker withdraws and claims SABLE; schedule+execute updateRewardPerSec(0); victim withdraws; assert attacker SABLE >> victim SABLE for equal deposit duration.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> SystemState.setCCR(lower)] Can an attacker with EXECUTOR_ROLE schedule and execute SystemState.setCCR(value_just_above_MCR) to raise the threshold at which Recovery Mode triggers, preventing Recovery Mode from activating when the system is undercollateralized, allowing the attacker to borrow more USDS than the system's true collateral supports, violating the invariant that CCR must protect the system from undercollateralization, causing scoped impact: attacker opens or adjusts a trove to borrow excess USDS that would have been blocked by the original CCR, extracting USDS debt value without adequate collateral backing? Local proof idea: schedule setCCR(1.11e18) where MCR=1.10e18; execute; assert CCR lowered; attempt to open trove at ICR between old CCR and new CCR; assert borrow succeeds; assert system TCR < original CCR.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _schedule() / delay parameter not upper-bounded] Can an attacker with PROPOSER_ROLE schedule a legitimate governance operation with delay = type(uint256).max - block.timestamp, causing SafeMath.add(block.timestamp, delay) to produce a timestamp near type(uint256).max, making the operation permanently unexecutable and blocking any future governance action that depends on this operation as a predecessor, violating the invariant that scheduled operations must eventually be executable or cancellable without permanently locking governance, causing scoped impact: if a critical reward-rate or MCR update is blocked, depositors cannot receive updated SABLE rewards or liquidation protections, and the attacker who holds a competing position benefits from the frozen state? Local proof idea: schedule op with delay=type(uint256).max - block.timestamp - 1; assert _timestamps[id] == type(uint256).max; assert isOperationReady never returns true; assert cancel() by proposer is the only remedy.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: cancel() / PROPOSER_ROLE same as scheduler] Can an attacker who holds PROPOSER_ROLE schedule a legitimate governance operation (e.g., setMCR increase to protect the system), then immediately cancel it before execution, repeatedly blocking protective parameter changes while leaving the system in a state where the attacker's pre-positioned StabilityPool deposit or staking position benefits from the unchanged parameters, violating the invariant that governance operations protecting reward fairness must not be cancellable by the same party that schedules them without additional authorization, causing scoped impact: attacker repeatedly cancels MCR-increase operations, keeping MCR low so their trove remains safe while others are liquidated, and they claim excess StabilityPool BNB gains? Local proof idea: grant PROPOSER_ROLE to attacker; schedule setMCR(higher); immediately cancel; repeat N times; assert MCR never updated; assert attacker trove never liquidated while victim troves are.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> SystemState.setUSDSGasCompensation(0)] Can an attacker with EXECUTOR_ROLE schedule and execute SystemState.setUSDSGasCompensation(0), eliminating the USDS gas compensation that liquidators receive from GasPool, making liquidations economically unprofitable for honest liquidators, while the attacker (who has pre-positioned as a large StabilityPool depositor) can still profitably liquidate using MEV or subsidized gas, violating the invariant that liquidation incentives must remain sufficient for all eligible liquidators, causing scoped impact: attacker monopolizes liquidation gains (BNB collateral) from StabilityPool offsets by being the only economically rational liquidator after gas compensation is removed? Local proof idea: schedule+execute setUSDSGasCompensation(0); assert GasPool compensation = 0; attempt liquidation from honest liquidator; assert net profit negative; assert attacker with MEV still profitable; assert attacker captures all BNB liquidation gains.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> SystemState.setUSDSGasCompensation(very_large)] Can an attacker with EXECUTOR_ROLE schedule and execute SystemState.setUSDSGasCompensation(very_large_value), causing every new trove opening to lock an excessive USDS amount in GasPool, and then liquidate those troves to claim the inflated gas compensation, violating the invariant that USDSGasCompensation must not exceed the protocol's intended 10 USDS per trove, causing scoped impact: attacker opens troves under the new high-compensation regime, gets liquidated (or self-liquidates via a controlled price), and claims the inflated USDS gas compensation from GasPool as excess reward? Local proof idea: schedule+execute setUSDSGasCompensation(1000e18); open trove; manipulate to liquidation; assert liquidator receives 1000 USDS from GasPool; assert GasPool drained beyond intended 10 USDS per trove.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _beforeCall() predecessor check / dependency chain manipulation] Can an attacker with PROPOSER_ROLE schedule operation A (setMCR lower, predecessor=0) and operation B (setRedemptionFeeFloor=0, predecessor=A.id), execute A to satisfy B's dependency, then execute B to eliminate redemption fees, violating the invariant that chained governance operations must not allow an attacker to atomically remove multiple reward-generating fee mechanisms in sequence, causing scoped impact: attacker redeems USDS for BNB with zero redemption fee after the chain executes, extracting BNB collateral value that should have been partially distributed as staking rewards? Local proof idea: schedule A and B with predecessor chain; execute A; assert B.predecessor satisfied; execute B; assert redemptionFeeFloor == 0; redeem USDS; assert zero BNB fee sent to SableStakingV2.increaseF_BNB.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() payable / value forwarding to SystemState] Can an attacker with EXECUTOR_ROLE call execute() with msg.value > 0 when the target is SystemState (which has no payable fallback), causing the call to revert inside _call() and the BNB sent with execute() to be trapped in the TimeLock contract, then schedule a subsequent operation to extract that trapped BNB to an attacker-controlled address, violating the invariant that BNB sent to execute() must not be extractable by unauthorized parties, causing scoped impact: attacker extracts BNB from TimeLock that was inadvertently sent during a failed governance execution? Local proof idea: call execute{value: 1 ether}() targeting SystemState; assert call reverts; assert TimeLock.balance increased by 1 ether; schedule call to attacker with value=1 ether; execute; assert attacker received 1 ether.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> reentrancy via _call() external call] Can an attacker with EXECUTOR_ROLE schedule an operation where target is an attacker-controlled contract that, upon receiving the _call(), re-enters execute() on a different pre-scheduled operation (different id) that is also ready, causing both operations to execute within the same transaction before either _afterCall() marks them done, violating the invariant that each operation must be independently validated as ready and marked done before any other operation executes, causing scoped impact: attacker executes two parameter changes (e.g., setMCR and setRedemptionFeeFloor) atomically without the system having a chance to process intermediate state, enabling reward extraction from the combined effect? Local proof idea: schedule op1 (setMCR) and op2 (setRedemptionFeeFloor) both ready; deploy ReentrantTarget that calls execute(op2) in its fallback; execute op1 targeting ReentrantTarget; assert both ops executed in same tx; assert both _timestamps set to _DONE_TIMESTAMP.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _afterCall() / isOperationReady check after external call] Can an attacker with EXECUTOR_ROLE schedule an operation where the target contract's execution modifies _timestamps[id] (by calling back into TimeLock.updateDelay or another self-referential path) such that isOperationReady(id) returns false when _afterCall() checks it, causing execute() to revert after the external call already had side effects, violating the invariant that _afterCall must always succeed if _call succeeded, causing scoped impact: the external call's side effects (e.g., a partial SystemState change) persist while the operation is not marked done, allowing it to be re-executed later for a double application of the parameter change? Local proof idea: deploy target that calls TimeLock.updateDelay(very_large) in its body; schedule op targeting this contract; execute; assert _afterCall reverts; assert _timestamps[id] still shows pending; assert op can be re-executed after delay.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: schedule() / isOperation check / _DONE_TIMESTAMP collision] Can an attacker observe that _DONE_TIMESTAMP = 1 and attempt to schedule an operation whose computed id maps to a _timestamps entry that is already 1 (done), noting that _schedule() checks !isOperation(id) which requires getTimestamp(id) > 0, and since a done operation has timestamp == 1 > 0, the re-schedule would revert, but can the attacker craft inputs such that a new operation's id collides with a done operation's id to prevent that operation from ever being re-scheduled, violating the invariant that completed operations must not block future scheduling of semantically identical operations with different salts, causing scoped impact: critical reward-rate governance operations are permanently blocked from re-execution? Local proof idea: execute op A; assert _timestamps[A.id] == 1; attempt schedule with same (target,value,data,predecessor,salt); assert revert 'already scheduled'; assert different salt produces new schedulable id.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: scheduleBatch() / length mismatch check] Can an attacker with PROPOSER_ROLE call scheduleBatch() with targets.length != values.length or targets.length != datas.length to trigger a revert, but more critically, can they pass a batch where one of the targets is address(0) or a non-contract address, causing _call() to succeed (since a call to EOA with empty data succeeds in EVM) and the batch to execute partially-meaningful operations, violating the invariant that all batch targets must be valid protocol contracts, causing scoped impact: attacker includes a no-op call to address(0) alongside a real SystemState parameter change to obscure the batch's true effect during the governance delay review window? Local proof idea: scheduleBatch([SystemState, address(0)], [0,0], [setMCR_data, ''], 0, salt, delay); warp; executeBatch; assert setMCR executed; assert address(0) call succeeded silently; assert MCR changed without obvious batch intent.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> SableStakingV2 via TimeLock ownership] Can an attacker with EXECUTOR_ROLE (assuming TimeLock holds TIMELOCK_ADMIN_ROLE and can grant itself EXECUTOR_ROLE via a self-referential operation) schedule and execute a call to SableStakingV2.increaseF_SABLE(large_amount) by impersonating the sableRewarder role check, violating the invariant that F_SABLE accumulator must only increase via authorized SableRewarder calls, causing scoped impact: attacker inflates F_SABLE before staking, then stakes and immediately unstakes to claim the inflated SABLE gain that was not backed by real protocol revenue? Local proof idea: if TimeLock can call increaseF_SABLE directly (bypassing _requireCallerIsSableRewarder), stake 1 wei LP; execute increaseF_SABLE(1e30) via TimeLock; unstake; assert SABLEGain = 1e30 * 1 / totalStaked; assert SABLE transferred from SableStakingV2 balance.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> SystemState.setMinNetDebt(1)] Can an attacker with EXECUTOR_ROLE schedule and execute SystemState.setMinNetDebt(1) to allow dust troves with near-zero debt, then open thousands of dust troves to manipulate SortedTroves ordering and StabilityPool offset calculations, violating the invariant that minNetDebt must prevent dust positions that distort reward accounting, causing scoped impact: attacker opens dust troves that are immediately liquidatable, each generating a tiny StabilityPool offset that accumulates rounding errors in the P/S/G accumulators, eventually allowing the attacker to claim more BNB or SABLE than their deposit entitles them to? Local proof idea: schedule+execute setMinNetDebt(1); open N dust troves; liquidate all; measure P accumulator drift; assert getDepositorBNBGain(attacker) > theoretical_entitlement due to rounding.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: hashOperation() / salt manipulation for operation ordering] Can an attacker with PROPOSER_ROLE schedule two operations with the same target/value/data/predecessor but different salts, where op1 (salt=A) sets MCR lower and op2 (salt=B) sets MCR even lower, execute them in sequence to achieve a double MCR reduction that would not have been possible in a single operation (due to _setMCR validation requiring MCR > 1e18 and MCR < CCR), violating the invariant that each governance action must independently satisfy parameter validation, causing scoped impact: attacker achieves a final MCR value that bypasses the single-step validation boundary, making more troves liquidatable and enabling excess StabilityPool BNB gain extraction? Local proof idea: schedule op1 setMCR(1.05e18) and op2 setMCR(1.01e18) with different salts; execute op1; assert MCR=1.05e18; execute op2; assert MCR=1.01e18; assert troves with ICR between 1.01e18 and 1.10e18 now liquidatable.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() / no upper bound on SystemState.setBorrowingFeeFloor] Can an attacker with EXECUTOR_ROLE schedule and execute SystemState.setBorrowingFeeFloor(DECIMAL_PRECISION) to set the borrowing fee floor to 100%, making all new USDS borrowing cost 100% in fees sent to SableStakingV2, then stake SABLE_LP just before execution to capture the massive fee inflow, violating the invariant that staking rewards must reflect genuine protocol revenue at reasonable fee rates, causing scoped impact: attacker stakes LP tokens, executes the fee floor change, any borrower who opens a trove pays 100% fee to SableStakingV2, attacker claims disproportionate USDS reward via F_USDS accumulator? Local proof idea: stake LP; schedule+execute setBorrowingFeeFloor(1e18); open trove with 100 USDS; assert USDS fee = 100 USDS sent to staking; assert attacker F_USDS gain = 100 USDS * stake / totalStaked.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> arbitrary target with USDS mint selector] Can an attacker with EXECUTOR_ROLE schedule and execute a call to USDSToken with data encoding mint(attacker, large_amount), bypassing USDSToken's _requireCallerIsBorrowerOperations check only if TimeLock is listed as an authorized minter, violating the invariant that USDS minting must only occur via BorrowerOperations with valid collateral, causing scoped impact: attacker mints unbacked USDS and deposits it into StabilityPool to claim BNB liquidation gains without providing any collateral? Local proof idea: check if TimeLock address is in USDSToken's authorized minters; if so, schedule execute(USDSToken, 0, abi.encodeCall(mint, (attacker, 1e24))); execute; assert attacker USDS balance increased; deposit into SP; liquidate trove; claim BNB gain."
]