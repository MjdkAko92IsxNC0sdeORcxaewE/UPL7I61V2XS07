[
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _descendList] Can an unprivileged ATTACKER (borrower or redeemer) under PRECONDITIONS where the sorted list is non-empty and the attacker supplies a valid prevId hint whose on-chain NICR equals the target NICR (so the hint passes the _findInsertPosition guard) but all subsequent nodes have lower NICR, trigger CALL_SEQUENCE openTrove/adjustTrove → _adjustTrove → sortedTroves.reInsert → _findInsertPosition → _descendList traverses to tail, sets prevId = data.nodes[tail].nextId = address(0), then nextId = data.nodes[address(0)].nextId = address(0), returns (address(0), address(0)) on a non-empty list, violating INVARIANT that _findInsertPosition must return a valid (prevId, nextId) pair consistent with the non-empty list state, causing scoped impact: _insert receives (address(0), address(0)) and overwrites data.head and data.tail with the attacker's trove address without linking to existing nodes, orphaning all other troves from getLast()/getFirst() traversal, enabling liquidateTroves to skip all genuinely undercollateralized troves (they are unreachable via getLast()) while the attacker's healthy trove sits at tail, creating protocol insolvency where bad debt accumulates without liquidation? Local proof idea: write a Foundry invariant test with 5 troves at descending NICRs, call adjustTrove on trove-1 with prevId hint = trove-5 (tail), assert that after reInsert, data.head and data.tail are not both equal to trove-1, and assert sortedTroves.getSize() equals the count of nodes reachable by traversing from getLast() via getPrev().",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _ascendList] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where the sorted list is non-empty and the attacker supplies a valid nextId hint whose on-chain NICR equals the target NICR but all preceding nodes have higher NICR, trigger CALL_SEQUENCE adjustTrove → _ascendList traverses to head, sets nextId = data.nodes[head].prevId = address(0), then prevId = data.nodes[address(0)].prevId = address(0), returns (address(0), address(0)) on a non-empty list, violating INVARIANT that _ascendList must return (address(0), head) when the new NICR is highest in the list, causing scoped impact: _insert overwrites data.head and data.tail with the attacker's trove, orphaning all existing nodes, so getLast() returns the attacker's high-NICR trove, liquidateTroves attempts to liquidate it (fails ICR check), and all genuinely undercollateralized troves are never reached, allowing insolvent troves to persist and drain protocol collateral through bad debt redistribution? Local proof idea: Foundry fuzz test with N troves, call reInsert with nextId = head and newNICR > head's NICR, assert data.head != param.id after the call and assert all N+1 nodes are reachable from getLast().",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _insert / _descendList] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where _findInsertPosition returns (address(0), address(0)) for a non-empty list (due to _descendList off-by-one), trigger CALL_SEQUENCE _insert executes the branch 'if (prevId == address(0) && nextId == address(0)) { data.head = param.id; data.tail = param.id; }' without checking isEmpty(), violating INVARIANT that the (address(0), address(0)) branch in _insert is only valid when the list is empty, causing scoped impact: the new node becomes both head and tail with nextId=address(0) and prevId=address(0), the old head's prevId is never set to the new node, and the old tail's nextId is never set to address(0) (it already is), so the list is split into two disconnected segments — the attacker's node and the original chain — enabling the attacker to control which trove getLast() returns and thus which trove redeemCollateral starts from, allowing the attacker to redirect redemptions away from their own trove? Local proof idea: unit test in Foundry: insert 3 nodes A(NICR=300), B(NICR=200), C(NICR=100); call insert for D(NICR=50) with prevId=C, nextId=address(0); verify _descendList is called with _startId=C; verify returned (prevId,nextId) is (C, address(0)) not (address(0), address(0)); then craft a scenario where NICR of C changes between hint computation and insertion to trigger the bug.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _descendList / _validInsertPosition] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where getNominalICR for list nodes includes pending redistribution rewards (L_BNB, L_USDSDebt accumulators) that have grown since the attacker's hint was computed, trigger CALL_SEQUENCE: (1) liquidation event increases L_BNB and L_USDSDebt, reducing effective NICRs of all active troves; (2) attacker calls adjustTrove with a prevId hint that was valid before the liquidation but now has a lower NICR than the attacker's newNICR; (3) _findInsertPosition nulls prevId because _NICR > getNominalICR(prevId); (4) _descendList starts from head; (5) if head's NICR also dropped below newNICR, returns (address(0), head) correctly — but if the attacker crafts the hint so that _findInsertPosition descends from a mid-list node whose NICR is now below newNICR, the traversal goes off the end, violating INVARIANT that _descendList must not return (address(0), address(0)) for a non-empty list, causing scoped impact: list corruption enabling liquidation bypass of insolvent troves? Local proof idea: Foundry test: set up 3 troves, trigger a liquidation to update L_BNB/L_USDSDebt, then call adjustTrove with a stale prevId hint; assert the resulting list is fully traversable from getLast() to getFirst().",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: reInsert / _remove / _insert] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where reInsert is called during redeemCollateral for a partial redemption, trigger CALL_SEQUENCE: (1) _remove(param.id) succeeds, removing the trove from the list and decrementing data.size; (2) _insert is called with the new NICR and caller-supplied hints; (3) _insert calls _findInsertPosition which calls _descendList; (4) _descendList traverses off the end due to the off-by-one bug and returns (address(0), address(0)); (5) _insert sets data.head = data.tail = param.id, orphaning all other nodes, violating INVARIANT that after reInsert the list must contain exactly the same set of troves as before (minus any closed ones) and all must be reachable from getLast(), causing scoped impact: subsequent redeemCollateral calls start from getLast() = the partially-redeemed trove (which has high NICR after partial redemption), skip all lower-NICR troves, and the redeemer cannot redeem from genuinely low-NICR troves, while the attacker's trove (now at tail) is the only redemption target — but since it has high NICR it may be skipped, effectively blocking redemption and protecting the attacker's collateral from redemption? Local proof idea: Foundry test with 5 troves, trigger partial redemption on trove-3, supply hints that cause _descendList to traverse off the end, assert all 4 remaining troves are reachable from getLast().",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _validInsertPosition] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where multiple troves have identical NICRs, trigger CALL_SEQUENCE: (1) attacker opens trove with NICR = X; (2) _validInsertPosition accepts any position between two nodes both with NICR = X (since the check is >= on both sides); (3) attacker places their trove immediately before a victim trove with the same NICR = X at the tail; (4) victim trove is now at the tail (getLast()); (5) liquidateTroves calls getLast() and liquidates the victim trove first, violating INVARIANT that among equal-NICR troves the ordering must not be manipulable by an unprivileged actor to force liquidation of a specific trove, causing scoped impact: attacker forces liquidation of a specific victim trove (e.g., a competitor's trove) while protecting their own equal-NICR trove from liquidation, gaining gas compensation at the victim's expense? Local proof idea: Foundry unit test with 3 troves all at NICR=200, attacker inserts with prevId=victim, assert victim is at tail, call liquidateTroves(1) and assert victim is liquidated not attacker.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _validInsertPosition / reInsert] Can an unprivileged ATTACKER (redeemer) under PRECONDITIONS where a partial redemption leaves a trove with a new NICR equal to an adjacent trove's NICR, trigger CALL_SEQUENCE: (1) redeemCollateral partially redeems trove-A, computing newNICR = X; (2) reInsert is called with upperPartialRedemptionHint and lowerPartialRedemptionHint supplied by the redeemer; (3) _validInsertPosition accepts the position between two nodes both with NICR = X; (4) the redeemer places trove-A immediately before trove-B (same NICR) at the tail; (5) subsequent redeemCollateral starts from getLast() = trove-A and redeems from it again, violating INVARIANT that a partially-redeemed trove must be reinserted at a position that reflects its true NICR ordering relative to all other troves, causing scoped impact: the redeemer can repeatedly target the same trove for redemption by manipulating its position to always be at the tail, extracting more BNB than they would from a fair ordering? Local proof idea: Foundry test with troves at NICRs [300, 200, 200, 100], partially redeem the NICR=200 trove, supply hints placing it at tail, assert getLast() is not the partially-redeemed trove.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: insert / _insert] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where the list is at maxSize - 1 (one slot remaining), trigger CALL_SEQUENCE: (1) attacker opens a trove, calling insert with newNICR and hints that cause _descendList to return (address(0), address(0)); (2) _insert sets data.head = data.tail = attacker's address; (3) data.size is incremented to maxSize; (4) isFull() returns true; (5) no new troves can be opened (insert reverts with 'List is full'), violating INVARIANT that the list must contain exactly data.size nodes all reachable from head/tail, causing scoped impact: the protocol is effectively frozen for new borrowers (DoS), but more critically, all existing troves are orphaned from getLast() traversal, so liquidateTroves cannot liquidate any undercollateralized trove, allowing insolvent borrowers to retain their collateral and USDS while the protocol accumulates bad debt? Local proof idea: Foundry test: fill list to maxSize-1, trigger the _descendList bug on the last insert, assert data.size == maxSize but only 1 node is reachable from getLast().",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _descendList (lines 337-342)] Can an unprivileged ATTACKER under PRECONDITIONS where _startId is the tail node and _NICR < getNominalICR(_startId) (so the new node should be inserted after the tail), trigger CALL_SEQUENCE: _descendList checks 'data.head == _startId && _NICR >= getNominalICR(_startId)' — false since _startId is tail not head; enters while loop with prevId=tail, nextId=data.nodes[tail].nextId=address(0); _validInsertPosition(NICR, tail, address(0)) checks 'data.tail == tail && NICR <= getNominalICR(tail)' — false since NICR < getNominalICR(tail) means NICR is smaller, so this IS valid (NICR <= getNominalICR(tail)); wait, NICR < getNominalICR(tail) means the new node has lower NICR than tail, so it should go after the tail — _validInsertPosition returns true, loop exits, returns (tail, address(0)) correctly. Now test the opposite: _NICR > getNominalICR(tail) — _validInsertPosition returns false, loop advances: prevId = data.nodes[tail].nextId = address(0), nextId = data.nodes[address(0)].nextId = address(0), loop exits (prevId == address(0)), returns (address(0), address(0)) — WRONG, should return (address(0), head) since NICR is highest, violating INVARIANT that _descendList must find a valid position for any NICR value in a non-empty list, causing scoped impact: list corruption as described above? Local proof idea: unit test _descendList with _startId=tail and _NICR > all existing NICRs, assert return value is not (address(0), address(0)).",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _ascendList (lines 361-366)] Can an unprivileged ATTACKER under PRECONDITIONS where _startId is the head node and _NICR > getNominalICR(_startId) (new node has highest NICR, should be inserted before head), trigger CALL_SEQUENCE: _ascendList checks 'data.tail == _startId && _NICR <= getNominalICR(_startId)' — false since _startId is head not tail; enters while loop with nextId=head, prevId=data.nodes[head].prevId=address(0); _validInsertPosition(NICR, address(0), head) checks 'data.head == head && NICR >= getNominalICR(head)' — true since NICR > getNominalICR(head); loop exits, returns (address(0), head) — CORRECT. Now test: _NICR < getNominalICR(head) — _validInsertPosition returns false, loop advances: nextId = data.nodes[head].prevId = address(0), prevId = data.nodes[address(0)].prevId = address(0), loop exits (nextId == address(0)), returns (address(0), address(0)) — WRONG, should have found a valid position by descending, violating INVARIANT that _ascendList must find a valid position for any NICR in a non-empty list when starting from a node with higher NICR than target, causing scoped impact: list corruption? Local proof idea: unit test _ascendList with _startId=head and _NICR < all existing NICRs, assert return is not (address(0), address(0)).",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _findInsertPosition] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where both prevId and nextId hints are supplied but prevId's NICR has dropped below newNICR (due to pending rewards accumulation since hint was computed), trigger CALL_SEQUENCE: (1) _findInsertPosition nulls prevId because _NICR > getNominalICR(prevId); (2) nextId is still valid (contains(nextId) && _NICR >= getNominalICR(nextId)); (3) _ascendList is called starting from nextId; (4) if nextId is the head and _NICR > getNominalICR(head), _ascendList returns (address(0), head) — correct; (5) but if nextId is a mid-list node and _NICR > getNominalICR(nextId) but < getNominalICR(nextId.prevId), _ascendList should find the correct position; (6) however if _NICR > getNominalICR of all nodes above nextId, _ascendList traverses to head and returns (address(0), head) — correct; test whether _ascendList can return (address(0), address(0)) in this path, violating INVARIANT that _findInsertPosition always returns a valid position for a non-empty list, causing scoped impact: list corruption enabling fund extraction? Local proof idea: differential fuzz test comparing _findInsertPosition output against a reference implementation for all NICR values and hint combinations.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: reInsert / _remove] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where reInsert is called and _remove succeeds but _insert subsequently reverts (e.g., due to the list being full after _remove decremented size and then _insert tries to re-add), trigger CALL_SEQUENCE: (1) reInsert calls _remove(param.id) — data.size decrements, node is deleted; (2) _insert is called — but if isFull() check passes (size < maxSize after decrement), _insert proceeds; (3) if _insert reverts for any reason (e.g., _findInsertPosition returns a position that fails _validInsertPosition and the fallback also fails), the entire reInsert transaction reverts, violating INVARIANT that reInsert must be atomic — either the trove remains in the list at its old position or is moved to the new position, causing scoped impact: if reInsert reverts, the trove operation (adjustTrove or redemption) also reverts, which is safe; but if _remove succeeds and _insert fails silently (no revert), the trove is permanently removed from the sorted list while remaining active in TroveManager, making it unliquidatable via liquidateTroves? Local proof idea: Foundry test: mock a scenario where _insert fails after _remove, assert the trove is still in the sorted list or the entire transaction reverted.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _insert (lines 133-156)] Can an unprivileged ATTACKER under PRECONDITIONS where _findInsertPosition returns (address(0), address(0)) for a non-empty list, trigger CALL_SEQUENCE: _insert executes 'data.head = param.id; data.tail = param.id' without setting data.nodes[param.id].nextId or data.nodes[param.id].prevId (they remain address(0) from the mapping default), and without updating data.nodes[old_head].prevId or data.nodes[old_tail].nextId, violating INVARIANT that after any insert the list must be a valid doubly-linked list where head.prevId == address(0), tail.nextId == address(0), and every node is reachable from both head and tail, causing scoped impact: the old head and tail are now orphaned — getLast() returns the attacker's node, getFirst() returns the attacker's node, getPrev(attacker) = address(0), getNext(attacker) = address(0), so liquidateTroves iterates only the attacker's node and stops, all other undercollateralized troves are never liquidated, protocol becomes insolvent? Local proof idea: Foundry invariant test: after any sequence of insert/remove/reInsert operations, assert that the number of nodes reachable from getLast() via getPrev() equals data.size.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _getTotalsFromLiquidateTrovesSequence_NormalMode (TroveManager) / getLast (SortedTroves)] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where the sorted list has been corrupted so that getLast() returns the attacker's high-NICR trove, trigger CALL_SEQUENCE: (1) liquidateTroves(N) calls sortedTrovesCached.getLast() = attacker's trove; (2) getCurrentICR(attacker, price) >= MCR, so the loop breaks immediately; (3) all genuinely undercollateralized troves (orphaned in the list) are never liquidated; (4) their debt continues to accumulate as bad debt; (5) the protocol's collateral ratio deteriorates, violating INVARIANT that liquidateTroves must liquidate all troves with ICR < MCR that are reachable in the sorted list, causing scoped impact: insolvent borrowers retain their collateral and USDS while the protocol accumulates bad debt, eventually making the system insolvent and allowing the insolvent borrowers to extract value (keep collateral they should have lost)? Local proof idea: Foundry test: corrupt the list so getLast() returns a healthy trove, call liquidateTroves(100), assert that all troves with ICR < MCR were liquidated.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: redeemCollateral (TroveManager) / getLast / getPrev (SortedTroves)] Can an unprivileged ATTACKER (redeemer) under PRECONDITIONS where the sorted list has been corrupted so that getLast() returns the attacker's trove (which has high NICR and thus high ICR), trigger CALL_SEQUENCE: (1) redeemCollateral with _firstRedemptionHint = address(0) (invalid hint); (2) currentBorrower = sortedTroves.getLast() = attacker's trove; (3) while loop checks getCurrentICR(attacker, price) < MCR — false (attacker has high ICR); (4) currentBorrower = sortedTroves.getPrev(attacker) = address(0) (attacker's node has no prev in corrupted list); (5) loop exits with currentBorrower = address(0); (6) totals.totalBNBDrawn = 0, require(totals.totalBNBDrawn > 0) reverts; (7) redemption is completely blocked, violating INVARIANT that redeemCollateral must be able to redeem from any trove with ICR >= MCR in the sorted list, causing scoped impact: redemption is blocked, USDS holders cannot redeem for BNB, the USDS peg breaks, and the attacker (who holds USDS) can buy USDS at a discount and later redeem when the list is fixed? Local proof idea: Foundry test: corrupt list, call redeemCollateral, assert it either succeeds or reverts with a meaningful error, not silently skips all redeemable troves.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _getTotalsFromLiquidateTrovesSequence_RecoveryMode / getPrev (SortedTroves)] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where the sorted list is corrupted and the system is in recovery mode, trigger CALL_SEQUENCE: (1) _getTotalsFromLiquidateTrovesSequence_RecoveryMode calls sortedTroves.getLast() and sortedTroves.getFirst(); (2) if getLast() == getFirst() (corrupted list with single node), the loop condition 'vars.user != vars.firstUser' is immediately false; (3) the loop does not execute; (4) totals.totalDebtInSequence = 0; (5) assert(totals.totalDebtInSequence > 0) reverts; (6) liquidateTroves reverts entirely, violating INVARIANT that liquidateTroves must succeed when there are undercollateralized troves in recovery mode, causing scoped impact: undercollateralized troves cannot be liquidated in recovery mode, the system remains in recovery mode indefinitely, and insolvent borrowers retain their collateral? Local proof idea: Foundry test: corrupt list to single-node state, enter recovery mode, call liquidateTroves, assert it either liquidates undercollateralized troves or provides a meaningful revert.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: insert / _requireCallerIsBOorTroveM] Can an unprivileged ATTACKER under PRECONDITIONS where the attacker is neither BorrowerOperations nor TroveManager, trigger CALL_SEQUENCE: attacker directly calls sortedTroves.insert() with arbitrary id, newNICR, prevId, nextId, violating INVARIANT that only BorrowerOperations and TroveManager can modify the sorted list, causing scoped impact: attacker inserts a fake trove address into the sorted list, corrupting getLast() and getFirst() traversal, enabling liquidation bypass or redemption manipulation? Local proof idea: Foundry test: call sortedTroves.insert() from an EOA, assert it reverts with 'SortedTroves: Caller is neither BO nor TroveM'.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _validInsertPosition (lines 303-318)] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where prevId and nextId are the same address (prevId == nextId), trigger CALL_SEQUENCE: _validInsertPosition checks 'data.nodes[prevId].nextId == nextId' which becomes 'data.nodes[prevId].nextId == prevId' — this is false unless the node points to itself (which is impossible in a valid list); so _validInsertPosition returns false, _findInsertPosition is called, and the correct position is found; but what if prevId == nextId == address(0)? Then _validInsertPosition returns isEmpty() — if list is non-empty, returns false, _findInsertPosition descends from head — correct behavior; violating INVARIANT that _validInsertPosition must correctly reject invalid hint pairs, causing scoped impact: if _validInsertPosition incorrectly accepts prevId == nextId, a trove could be inserted in an invalid position, corrupting the list? Local proof idea: unit test _validInsertPosition with prevId == nextId for various list states, assert it always returns false for non-empty lists.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _validInsertPosition / getNominalICR] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where a trove has pending redistribution rewards that have not been applied (hasPendingRewards = true), trigger CALL_SEQUENCE: (1) attacker calls adjustTrove without triggering applyPendingRewards for a neighbor trove; (2) _validInsertPosition calls getNominalICR(prevId) which includes pending rewards for prevId (higher effective debt, lower NICR); (3) the attacker's newNICR is computed from their own trove's state (after applyPendingRewards is called for the attacker in _adjustTrove); (4) the comparison uses different bases — attacker's NICR is post-reward, neighbor's NICR is pre-reward-application but post-reward-accumulation; (5) this could allow the attacker to be inserted at a position that is incorrect relative to the neighbor's true NICR after reward application, violating INVARIANT that the list ordering must reflect true NICRs including pending rewards, causing scoped impact: attacker's trove is placed above a trove that should be above it, protecting the attacker from being the first liquidation target? Local proof idea: Foundry test with pending rewards, call adjustTrove, assert the resulting list ordering matches the ordering by getNominalICR for all troves.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _descendList (lines 327-343)] Can an unprivileged ATTACKER under PRECONDITIONS where _startId is not the head and _NICR >= getNominalICR(_startId) (so the new node should be inserted before _startId), trigger CALL_SEQUENCE: _descendList checks 'data.head == _startId && _NICR >= getNominalICR(_startId)' — false since _startId is not head; enters while loop with prevId=_startId, nextId=data.nodes[_startId].nextId; _validInsertPosition(NICR, _startId, nextId) — if NICR >= getNominalICR(_startId) and NICR >= getNominalICR(nextId), this is valid only if getNominalICR(_startId) >= NICR, which contradicts NICR >= getNominalICR(_startId) unless equal; so if NICR > getNominalICR(_startId), _validInsertPosition returns false; loop advances: prevId = data.nodes[_startId].nextId (moves DOWN the list, away from the correct position); the traversal goes in the wrong direction, violating INVARIANT that _descendList must find the correct insert position when starting from a node with lower NICR than the target, causing scoped impact: the trove is inserted at the wrong position (too low in the list), making it appear to have lower NICR than it actually does, causing it to be liquidated before higher-NICR troves? Local proof idea: unit test _descendList with _startId having lower NICR than _NICR, assert the returned position is correct (i.e., the new node would be above _startId).",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: reInsert / _validInsertPosition] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where the attacker calls adjustTrove to increase their NICR (add collateral), trigger CALL_SEQUENCE: (1) _adjustTrove computes newNICR = higher value; (2) reInsert is called with newNICR and hints; (3) _remove removes the attacker's trove from its current position; (4) _insert tries to insert at the new (higher) position; (5) if the hints point to a position that is now invalid (because the attacker's removal changed the list structure — e.g., the attacker was between prevId and nextId, and after removal prevId.nextId now points to nextId directly), _validInsertPosition might return a different result than expected; (6) _findInsertPosition is called and finds the correct position; but test whether the removal of the attacker's node can cause _validInsertPosition to incorrectly validate a wrong position, violating INVARIANT that reInsert must place the trove at the correct NICR-ordered position, causing scoped impact: attacker's trove is placed at a position that makes it appear to have lower NICR, causing it to be liquidated before it should be? Local proof idea: Foundry test: insert A(300), B(200), C(100); reInsert B with newNICR=350 and hints prevId=A, nextId=C; assert B is now at head.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _insert (lines 137-141)] Can an unprivileged ATTACKER under PRECONDITIONS where _findInsertPosition returns (address(0), nextId) where nextId is NOT the current head, trigger CALL_SEQUENCE: _insert executes 'data.nodes[param.id].nextId = data.head; data.nodes[data.head].prevId = param.id; data.head = param.id' — this inserts the new node before the current head, regardless of what nextId was; but if nextId != data.head, the new node is inserted at the wrong position (before head instead of before nextId), violating INVARIANT that when prevId == address(0), the new node must be inserted before nextId (which should be the head), causing scoped impact: the new node is placed at the head even though its NICR might be lower than the current head's NICR, corrupting the list ordering and potentially causing the attacker's trove to be treated as having the highest NICR (first() returns it), protecting it from redemption? Local proof idea: unit test _insert with prevId=address(0) and nextId != head, assert the function either reverts or correctly inserts before nextId.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _insert (lines 142-146)] Can an unprivileged ATTACKER under PRECONDITIONS where _findInsertPosition returns (prevId, address(0)) where prevId is NOT the current tail, trigger CALL_SEQUENCE: _insert executes 'data.nodes[param.id].prevId = data.tail; data.nodes[data.tail].nextId = param.id; data.tail = param.id' — this inserts the new node after the current tail, regardless of what prevId was; but if prevId != data.tail, the new node is inserted at the wrong position (after tail instead of after prevId), violating INVARIANT that when nextId == address(0), the new node must be inserted after prevId (which should be the tail), causing scoped impact: the new node is placed at the tail even though its NICR might be higher than the current tail's NICR, making it the first liquidation target even though it has higher NICR than other troves? Local proof idea: unit test _insert with nextId=address(0) and prevId != tail, assert the function either reverts or correctly inserts after prevId.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _validInsertPosition (line 309)] Can an unprivileged ATTACKER (borrower) under PRECONDITIONS where the attacker supplies nextId = head but their newNICR < getNominalICR(head), trigger CALL_SEQUENCE: _validInsertPosition checks 'data.head == _nextId && _NICR >= _troveManager.getNominalICR(_nextId)' — false since NICR < getNominalICR(head); _findInsertPosition is called; since prevId was nulled (NICR > getNominalICR(prevId) was false, so prevId might still be valid), _descendList is called from prevId; if prevId is valid and NICR <= getNominalICR(prevId), _descendList descends correctly; but if prevId is also invalid, _descendList descends from head; test whether this path can produce an incorrect position, violating INVARIANT that the resulting position must satisfy getNominalICR(prevId) >= NICR >= getNominalICR(nextId), causing scoped impact: trove inserted at wrong position, affecting liquidation/redemption ordering? Local proof idea: fuzz test _validInsertPosition and _findInsertPosition with random NICR values and hint combinations, assert the returned position always satisfies the ordering invariant.",
  "[File: 'File Name: src/SortedTroves.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: _remove (lines 168-203)] Can an unprivileged ATTACKER under PRECONDITIONS where data.size == 1 (single node in list), trigger CALL_SEQUENCE: _remove checks 'data.size > 1' — false; executes 'data.head = address(0); data.tail = address(0)'; then 'delete data.nodes[_id]' sets exists=false; 'data.size = data.size.sub(1)' = 0; but 'NodeRemoved(_id)' is called as a function call (not emit), which in Solidity 0.6.11 is a no-op (events called as functions are silently ignored) — this is a minor issue; more critically, test whether _remove correctly handles the case where the single node has non-zero nextId or prevId (which should be impossible in a valid list), violating INVARIANT that after removing the last node, data.head == address(0) && data.tail == address(0) && data.size == 0, causing scoped impact: if the list state is inconsistent, subsequent insert calls might behave incorrectly? Local proof idea: unit test _remove on a single-node list, assert data.head == address(0), data.tail == address(0), data.size == 0."
]