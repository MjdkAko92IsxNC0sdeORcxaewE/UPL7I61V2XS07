[
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: DefaultPool.receive / TroveManager._redistributeDebtAndColl] Can an unprivileged ATTACKER (borrower with large stake) under PRECONDITIONS (many large troves exist, a tiny-collateral trove is liquidated such that collToRedistribute * DECIMAL_PRECISION + lastBNBError_Redistribution < totalStakes) trigger CALL_SEQUENCE (liquidate tiny-coll trove → _redistributeDebtAndColl sets BNBRewardPerUnitStaked=0 but USDSDebtRewardPerUnitStaked>0 → L_BNB unchanged, L_USDSDebt increases → attacker's rewardSnapshots[attacker].BNB == L_BNB so hasPendingRewards returns false → attacker calls closeTrove → applyPendingRewards skips → attacker repays only stored debt, not stored+pendingUSDSDebt), violating INVARIANT (every USDS mint must correspond to valid debt; closing a trove must burn all outstanding debt including redistributed debt), causing scoped impact: attacker closes trove underpaying redistributed USDS debt, leaving DefaultPool.USDSDebt permanently inflated and attacker extracting USDS value? Local proof idea: write Foundry unit test with N large troves and one dust-collateral trove; liquidate dust trove; assert BNBRewardPerUnitStaked==0 and L_USDSDebt increased; call closeTrove on a large trove; assert DefaultPool.USDSDebt > 0 after close and attacker's USDS balance exceeds what they should have retained.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager.hasPendingRewards / TroveManager._applyPendingRewards] Can an unprivileged ATTACKER (trove owner) under PRECONDITIONS (L_BNB has not changed since attacker's last snapshot but L_USDSDebt has increased due to a redistribution where BNBRewardPerUnitStaked rounded to zero) trigger CALL_SEQUENCE (attacker calls BorrowerOperations.withdrawUSDS → _adjustTrove → applyPendingRewards → hasPendingRewards returns false → pending USDSDebt reward not applied → trove debt understated → ICR check passes with understated debt → attacker borrows additional USDS against collateral that is already over-leveraged), violating INVARIANT (attacker must not borrow USDS while undercollateralized when pending debt is included), causing scoped impact: attacker mints USDS without corresponding collateral backing, extracting USDS value from the protocol? Local proof idea: Foundry fuzz test varying collToRedistribute from 1 wei to 1e15 wei with fixed large totalStakes; assert that after redistribution where BNBRewardPerUnitStaked==0, a trove can withdrawUSDS beyond its true collateral capacity.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._redistributeDebtAndColl / DefaultPool.increaseUSDSDebt] Can an unprivileged ATTACKER (liquidator) under PRECONDITIONS (system has many redistributions with small collateral amounts causing lastBNBError_Redistribution to accumulate near totalStakes) trigger CALL_SEQUENCE (attacker triggers multiple small liquidations → each redistribution adds to lastBNBError_Redistribution → eventually lastBNBError_Redistribution causes BNBRewardPerUnitStaked to spike on the next redistribution → active troves claim more BNB than was redistributed in that round → DefaultPool.BNB underflows on sendBNBToActivePool), violating INVARIANT (sum of all getPendingBNBReward across active troves must not exceed DefaultPool.BNB), causing scoped impact: DefaultPool.BNB underflow reverts legitimate operations or attacker extracts excess BNB? Local proof idea: Foundry invariant test asserting sum(getPendingBNBReward(t) for t in active_troves) <= defaultPool.getBNB() after every liquidation sequence with fuzzed collateral amounts.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._redistributeDebtAndColl] Can an unprivileged ATTACKER (large-stake trove owner) under PRECONDITIONS (attacker opens a trove with maximum collateral just before a large liquidation redistribution, so their stake is large relative to totalStakes) trigger CALL_SEQUENCE (attacker openTrove with large BNB → victim trove liquidated → _redistributeDebtAndColl distributes collToRedistribute across totalStakes including attacker's large stake → attacker's getPendingBNBReward is disproportionately large → attacker applyPendingRewards → attacker withdrawColl → attacker extracts more BNB than they deposited), violating INVARIANT (attacker must not extract more BNB than their collateral entitlement), causing scoped impact: attacker profits from redistribution by front-running liquidation with large stake? Local proof idea: Foundry unit test with attacker opening max-stake trove in same block as liquidation; assert attacker.BNB_after - attacker.BNB_before > 0 after withdrawColl.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._applyPendingRewards / DefaultPool.sendBNBToActivePool] Can an unprivileged ATTACKER (redeemer) under PRECONDITIONS (multiple troves have large pending BNB rewards from redistributions, DefaultPool.BNB is exactly equal to sum of all pending rewards) trigger CALL_SEQUENCE (attacker calls redeemCollateral → for each trove in redemption loop _applyPendingRewards is called → each call decrements DefaultPool.BNB via sendBNBToActivePool → rounding in getPendingBNBReward causes sum of applied rewards to exceed DefaultPool.BNB → SafeMath underflow reverts the entire redemption), violating INVARIANT (redemption must not revert due to DefaultPool.BNB exhaustion caused by rounding), causing scoped impact: attacker can grief redemptions or if rounding goes the other way extract excess BNB? Local proof idea: Foundry fuzz test with N troves and fuzzed stake/L_BNB values; assert that sequential _applyPendingRewards calls never cause DefaultPool.BNB to underflow.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._updateSystemSnapshots_excludeCollRemainder / TroveManager._computeNewStake] Can an unprivileged ATTACKER under PRECONDITIONS (DefaultPool.BNB is inflated by force-sending ETH via selfdestruct to DefaultPool, causing totalCollateralSnapshot to be inflated) trigger CALL_SEQUENCE (attacker deploys selfdestruct contract with ETH → selfdestruct to DefaultPool → DefaultPool.BNB tracker unchanged but address(defaultPool).balance increases → _updateSystemSnapshots_excludeCollRemainder reads defaultPool.getBNB() which returns the tracker not actual balance → totalCollateralSnapshot is NOT inflated → but sendBNBToActivePool sends from actual balance → mismatch), violating INVARIANT (DefaultPool.BNB tracker must equal actual ETH available for sendBNBToActivePool), causing scoped impact: force-sent ETH is permanently locked in DefaultPool, reducing available BNB for legitimate pending reward transfers? Local proof idea: Foundry unit test; selfdestruct ETH into DefaultPool; assert address(defaultPool).balance > defaultPool.getBNB(); then trigger sendBNBToActivePool for full BNB amount and verify it succeeds using actual balance.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._computeNewStake / TroveManager._updateSystemSnapshots_excludeCollRemainder] Can an unprivileged ATTACKER under PRECONDITIONS (totalCollateralSnapshot includes DefaultPool.BNB which has pending rewards not yet applied, causing totalCollateralSnapshot to be larger than activePool.BNB alone) trigger CALL_SEQUENCE (liquidation updates totalCollateralSnapshot = activePool.getBNB() - gasComp + defaultPool.getBNB() → new trove opens → _computeNewStake uses inflated totalCollateralSnapshot → attacker's stake is smaller than it should be → attacker receives fewer redistribution rewards → but attacker's ICR is computed using actual coll → attacker can borrow more USDS), violating INVARIANT (stake must accurately reflect trove's proportional claim on redistribution rewards), causing scoped impact: attacker's understated stake allows over-borrowing relative to their redistribution liability? Local proof idea: Foundry unit test comparing stake computed with and without DefaultPool.BNB in totalCollateralSnapshot; assert ICR-based borrow limit is consistent with stake-based reward liability.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._redistributeDebtAndColl / DefaultPool.increaseUSDSDebt + DefaultPool.sendBNBToActivePool] Can an unprivileged ATTACKER under PRECONDITIONS (two sequential liquidations in the same block, first redistribution sets lastBNBError_Redistribution to a large value, second redistribution uses that error in BNBNumerator) trigger CALL_SEQUENCE (liquidate trove A → _redistributeDebtAndColl updates L_BNB and lastBNBError_Redistribution → liquidate trove B in same tx → second _redistributeDebtAndColl adds lastBNBError_Redistribution to BNBNumerator → BNBRewardPerUnitStaked for second redistribution is inflated → active troves can claim more BNB than trove B's collateral), violating INVARIANT (sum of BNB distributed via L_BNB increments must equal sum of collToRedistribute across all liquidations), causing scoped impact: attacker extracts excess BNB from DefaultPool beyond redistributed collateral? Local proof idea: Foundry unit test with two sequential liquidations; assert L_BNB * totalStakes / DECIMAL_PRECISION <= sum(collToRedistribute) + initial_lastBNBError.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager.getPendingBNBReward / TroveManager.getPendingUSDSDebtReward] Can an unprivileged ATTACKER under PRECONDITIONS (attacker's trove snapshot was taken at L_BNB=X, L_USDSDebt=Y; multiple redistributions occur; attacker's stake is large) trigger CALL_SEQUENCE (attacker calls getEntireDebtAndColl → pendingBNBReward = stake * (L_BNB - X) / DECIMAL_PRECISION → pendingUSDSDebtReward = stake * (L_USDSDebt - Y) / DECIMAL_PRECISION → both computed independently with floor division → attacker applies pending rewards → BNB received may exceed USDSDebt applied proportionally), violating INVARIANT (BNB/USDS ratio of pending rewards must match the ratio of redistributed collateral/debt), causing scoped impact: attacker receives more BNB per unit of debt absorbed than the liquidated trove's actual collateral ratio? Local proof idea: Foundry fuzz test with fuzzed stake, L_BNB delta, L_USDSDebt delta; assert pendingBNBReward/pendingUSDSDebtReward <= original_liquidated_coll/original_liquidated_debt.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._applyPendingRewards / BorrowerOperations.closeTrove] Can an unprivileged ATTACKER under PRECONDITIONS (attacker has a trove with pending BNB rewards from redistribution, attacker has enough USDS to repay stored debt but not stored+pending debt) trigger CALL_SEQUENCE (attacker calls closeTrove → applyPendingRewards applies pendingBNBReward to trove.coll and pendingUSDSDebtReward to trove.debt → _movePendingTroveRewardsToActivePool moves BNB from DefaultPool to ActivePool → closeTrove sends trove.coll (now including pendingBNBReward) to attacker → attacker repays trove.debt (now including pendingUSDSDebtReward) → attacker receives pendingBNBReward as collateral), violating INVARIANT (this is correct behavior, but verify: does attacker receive more BNB than they deposited?), causing scoped impact: confirm whether redistribution reward receipt constitutes legitimate gain or exploitable over-extraction? Local proof idea: Foundry unit test asserting attacker.BNB_received == attacker.BNB_deposited + pendingBNBReward and pendingBNBReward <= their proportional share of redistributed collateral.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._redistributeDebtAndColl] Can an unprivileged ATTACKER under PRECONDITIONS (totalStakes is 1 wei, a liquidation redistributes a large amount of collateral) trigger CALL_SEQUENCE (all but one trove closed → totalStakes = 1 wei → liquidation of another trove → BNBRewardPerUnitStaked = _coll * DECIMAL_PRECISION / 1 = _coll * 1e18 → L_BNB increases by _coll * 1e18 → remaining trove's getPendingBNBReward = 1 * _coll * 1e18 / 1e18 = _coll → correct, but check overflow: _coll * DECIMAL_PRECISION could overflow uint256 if _coll > 1e59), violating INVARIANT (no overflow in L_BNB accumulation), causing scoped impact: overflow in L_BNB causes incorrect pending reward calculation, allowing attacker to claim 0 or wrap-around BNB? Local proof idea: Foundry fuzz test with totalStakes=1 and fuzzed _coll up to type(uint256).max/DECIMAL_PRECISION; assert no overflow in BNBNumerator computation.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._redistributeDebtAndColl / TroveManager.getPendingBNBReward] Can an unprivileged ATTACKER under PRECONDITIONS (attacker has large stake, L_BNB is near type(uint256).max due to many redistributions with small totalStakes) trigger CALL_SEQUENCE (many redistributions accumulate L_BNB → L_BNB approaches max uint256 → attacker's getPendingBNBReward = stake * (L_BNB - snapshot) / DECIMAL_PRECISION → if L_BNB - snapshot overflows, pendingBNBReward is computed incorrectly → attacker applies inflated pending rewards → drains DefaultPool.BNB), violating INVARIANT (L_BNB must not overflow; pending reward computation must not overflow), causing scoped impact: attacker extracts excess BNB from DefaultPool via overflow in reward accumulator? Local proof idea: Foundry fuzz test accumulating L_BNB over many redistributions; assert L_BNB never overflows and getPendingBNBReward never exceeds DefaultPool.BNB.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager.batchLiquidateTroves / TroveManager._getTotalsFromBatchLiquidate_NormalMode] Can an unprivileged ATTACKER (liquidator) under PRECONDITIONS (attacker includes a non-active trove address in the batch array) trigger CALL_SEQUENCE (batchLiquidateTroves called with array containing non-active trove → _getTotalsFromBatchLiquidate_NormalMode does NOT skip non-active troves (unlike RecoveryMode version which has the skip check) → getCurrentICR called on non-active trove → if ICR < MCR, _liquidateNormalMode called → getEntireDebtAndColl returns 0 debt/coll for non-active trove → _movePendingTroveRewardsToActivePool called with 0 amounts → _redistributeDebtAndColl called with 0 debt → returns early → but _closeTrove is called on already-closed trove), violating INVARIANT (liquidation must only process active troves), causing scoped impact: double-close of a trove corrupts TroveOwners array or causes unexpected state changes? Local proof idea: Foundry unit test calling batchLiquidateTroves with a mix of active and closed trove addresses in NormalMode; assert no state corruption.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._movePendingTroveRewardsToActivePool / DefaultPool.decreaseUSDSDebt] Can an unprivileged ATTACKER under PRECONDITIONS (DefaultPool.USDSDebt is less than the sum of all pending USDS debt rewards due to rounding in _redistributeDebtAndColl) trigger CALL_SEQUENCE (multiple troves apply pending rewards sequentially → each call to decreaseUSDSDebt subtracts pendingUSDSDebtReward → last trove's decreaseUSDSDebt causes SafeMath underflow because DefaultPool.USDSDebt < pendingUSDSDebtReward), violating INVARIANT (DefaultPool.USDSDebt must always be >= sum of pending USDS debt rewards for all active troves), causing scoped impact: legitimate trove operations revert due to DefaultPool.USDSDebt underflow, or if underflow is bypassed, accounting corruption enables over-borrowing? Local proof idea: Foundry invariant test asserting defaultPool.getUSDSDebt() >= sum(getPendingUSDSDebtReward(t) for t in active_troves) after every state-changing operation.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager.redeemCollateral / TroveManager._applyPendingRewards] Can an unprivileged ATTACKER (redeemer) under PRECONDITIONS (a trove being redeemed from has large pending BNB rewards in DefaultPool, DefaultPool.BNB is exactly equal to that trove's pendingBNBReward) trigger CALL_SEQUENCE (redeemCollateral → _applyPendingRewards for target trove → sendBNBToActivePool(pendingBNBReward) → DefaultPool.BNB becomes 0 → _redeemCollateralFromTrove reduces trove.coll by BNBLot → activePool.sendBNB(redeemer, BNBToSendToRedeemer) → redeemer receives BNB that includes the just-transferred pending reward), violating INVARIANT (redeemer must only receive BNB corresponding to USDS burned, not pending reward BNB that was moved from DefaultPool), causing scoped impact: redeemer extracts BNB that was pending reward for the trove owner, effectively stealing redistribution rewards? Local proof idea: Foundry unit test; set up trove with large pending reward; redeem against it; assert trove owner's claimable collateral surplus equals expected amount.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._liquidateNormalMode / TroveManager._movePendingTroveRewardsToActivePool] Can an unprivileged ATTACKER (liquidator) under PRECONDITIONS (victim trove has large pending BNB rewards, DefaultPool.BNB == pendingBNBReward, victim trove's entireTroveColl includes pendingBNBReward) trigger CALL_SEQUENCE (liquidate victim → _movePendingTroveRewardsToActivePool moves pendingBNBReward from DefaultPool to ActivePool → _getOffsetAndRedistributionVals uses entireTroveColl (including pendingBNBReward) → collToSendToSP or collToRedistribute includes the moved BNB → ActivePool.sendBNB to StabilityPool or DefaultPool → double-counting of BNB), violating INVARIANT (BNB moved from DefaultPool to ActivePool must not be double-counted in liquidation distribution), causing scoped impact: StabilityPool or active troves receive more BNB than the liquidated trove's actual collateral? Local proof idea: Foundry unit test; liquidate trove with pending rewards; assert totalBNBDistributed == entireTroveColl (not entireTroveColl + pendingBNBReward).",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._redistributeDebtAndColl / TroveManager._updateSystemSnapshots_excludeCollRemainder] Can an unprivileged ATTACKER under PRECONDITIONS (liquidation sequence processes multiple troves, totalCollateralSnapshot is updated after all liquidations using final activePool.BNB + defaultPool.BNB) trigger CALL_SEQUENCE (batch liquidation → _redistributeDebtAndColl called multiple times, each time using current totalStakes as denominator → totalStakesSnapshot set to final totalStakes → totalCollateralSnapshot set to final pool balances → new trove opens → _computeNewStake uses totalStakesSnapshot/totalCollateralSnapshot from after all liquidations → stake is computed correctly), violating INVARIANT (verify: does the snapshot update timing create any window where stake computation is incorrect?), causing scoped impact: if snapshot is stale during batch liquidation, intermediate redistributions use wrong totalStakes, causing over/under distribution? Local proof idea: Foundry unit test with batch liquidation of 3 troves; assert each _redistributeDebtAndColl uses the correct totalStakes at that point in the sequence.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._redistributeDebtAndColl] Can an unprivileged ATTACKER under PRECONDITIONS (attacker is the only remaining active trove after all others are liquidated, totalStakes equals attacker's stake) trigger CALL_SEQUENCE (all other troves liquidated → each liquidation redistributes to attacker → attacker's pending rewards accumulate → attacker applies pending rewards → attacker's trove.coll grows → attacker withdrawColl → attacker extracts all redistributed BNB), violating INVARIANT (last remaining trove must absorb all redistributed debt and collateral; verify this is correctly accounted), causing scoped impact: attacker as last trove extracts all redistributed BNB without paying redistributed debt? Local proof idea: Foundry unit test with 2 troves; liquidate one; assert remaining trove's pendingBNBReward == liquidated trove's collToRedistribute and pendingUSDSDebtReward == liquidated trove's debtToRedistribute.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: DefaultPool.sendBNBToActivePool / ActivePool.receive] Can an unprivileged ATTACKER under PRECONDITIONS (attacker controls a contract that can be set as activePoolAddress, which is fixed at deployment and cannot be changed) trigger CALL_SEQUENCE (N/A - activePoolAddress is set once in setAddresses and ownership is renounced → cannot be changed → sendBNBToActivePool always sends to the legitimate ActivePool), violating INVARIANT (verify: is there any path where sendBNBToActivePool sends BNB to an attacker-controlled address?), causing scoped impact: if activePoolAddress could be changed post-deployment, attacker could redirect DefaultPool BNB? Local proof idea: Foundry unit test confirming setAddresses can only be called once (ownership renounced after); assert subsequent setAddresses calls revert.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._redistributeDebtAndColl / TroveManager.getPendingBNBReward] Can an unprivileged ATTACKER under PRECONDITIONS (attacker opens a trove with 1 wei collateral, totalCollateralSnapshot is very large, so attacker's stake is near 0) trigger CALL_SEQUENCE (attacker openTrove with 1 wei → stake = 1 * totalStakesSnapshot / totalCollateralSnapshot ≈ 0 → liquidation redistributes large collateral → BNBRewardPerUnitStaked = large_coll * 1e18 / totalStakes → attacker's getPendingBNBReward = 0 * BNBRewardPerUnitStaked / 1e18 = 0 → attacker receives no redistribution reward → but attacker's trove.coll = 1 wei → attacker can still close trove and get 1 wei back), violating INVARIANT (dust-stake troves must not receive disproportionate rewards or cause accounting issues), causing scoped impact: dust-stake troves cause rounding losses in redistribution, effectively stealing from other troves? Local proof idea: Foundry fuzz test with attacker stake=1 wei and large redistribution; assert other troves receive correct proportional rewards.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._liquidateRecoveryMode / TroveManager._movePendingTroveRewardsToActivePool] Can an unprivileged ATTACKER under PRECONDITIONS (system in recovery mode, victim trove has ICR between MCR and TCR, victim trove has large pending rewards in DefaultPool) trigger CALL_SEQUENCE (liquidateInRecoveryMode → _movePendingTroveRewardsToActivePool moves pendingBNBReward from DefaultPool → getCappedOffsetVals computes collToSendToSP based on entireTroveColl (which includes pendingBNBReward) → collSurplus = entireTroveColl - cappedCollPortion → collSurplus includes portion of pendingBNBReward → victim can claim collSurplus → victim receives BNB that was their pending reward plus surplus), violating INVARIANT (collateral surplus must only include BNB above the capped liquidation amount, not double-count pending rewards), causing scoped impact: victim over-claims collateral surplus including pending reward BNB? Local proof idea: Foundry unit test in recovery mode; liquidate trove with pending rewards; assert collSurplusPool.getCollateral(victim) == expected surplus without double-counting.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager.getEntireSystemColl / LiquityBase.getEntireSystemColl] Can an unprivileged ATTACKER under PRECONDITIONS (DefaultPool.BNB is large due to many redistributions not yet applied) trigger CALL_SEQUENCE (attacker observes getEntireSystemColl = activePool.getBNB() + defaultPool.getBNB() is large → TCR = getEntireSystemColl * price / getEntireSystemDebt is above CCR → system not in recovery mode → attacker can open new trove or withdraw collateral that would otherwise be blocked in recovery mode → attacker extracts BNB or USDS), violating INVARIANT (TCR calculation must accurately reflect system solvency; DefaultPool.BNB represents pending rewards not yet applied, not free collateral), causing scoped impact: inflated DefaultPool.BNB prevents recovery mode, allowing attacker to borrow more USDS than system can support? Local proof idea: Foundry unit test; create large DefaultPool.BNB via redistributions; assert TCR with and without DefaultPool.BNB; verify recovery mode threshold is correctly computed.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._redistributeDebtAndColl] Can an unprivileged ATTACKER under PRECONDITIONS (attacker manipulates the order of liquidations to maximize their pending reward claim) trigger CALL_SEQUENCE (attacker opens large trove → attacker triggers liquidation of small trove A → L_BNB increases → attacker's snapshot is at old L_BNB → attacker triggers liquidation of small trove B → L_BNB increases again → attacker applies pending rewards for both redistributions → attacker's total pendingBNBReward = stake * (L_BNB_final - L_BNB_initial) / DECIMAL_PRECISION), violating INVARIANT (attacker's total pending reward must equal their proportional share of all redistributed collateral), causing scoped impact: verify whether ordering of liquidations affects attacker's total reward claim (it should not, but rounding may create differences)? Local proof idea: Foundry differential test comparing attacker reward when liquidating A then B vs B then A; assert rewards are equal within rounding tolerance.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager._applyPendingRewards / TroveManager._updateTroveRewardSnapshots] Can an unprivileged ATTACKER under PRECONDITIONS (attacker calls applyPendingRewards via BorrowerOperations.adjustTrove multiple times in sequence) trigger CALL_SEQUENCE (attacker adjustTrove → applyPendingRewards → _updateTroveRewardSnapshots sets snapshot to current L_BNB/L_USDSDebt → pending rewards applied and moved from DefaultPool → attacker adjustTrove again → hasPendingRewards returns false (snapshot == L_BNB) → no double-application), violating INVARIANT (pending rewards must not be applied twice), causing scoped impact: verify double-application is impossible; if snapshot update fails or is skipped, attacker could drain DefaultPool.BNB twice? Local proof idea: Foundry unit test calling adjustTrove twice in sequence; assert DefaultPool.BNB decremented exactly once by pendingBNBReward.",
  "[File: 'File Name: src/DefaultPool.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: TroveManager.updateTroveRewardSnapshots / BorrowerOperations.openTrove] Can an unprivileged ATTACKER under PRECONDITIONS (attacker opens a trove after a redistribution has occurred, so L_BNB > 0 at time of opening) trigger CALL_SEQUENCE (redistribution occurs → L_BNB = X → attacker openTrove → updateTroveRewardSnapshots sets rewardSnapshots[attacker].BNB = X → attacker's getPendingBNBReward = stake * (L_BNB - X) / DECIMAL_PRECISION = 0 → attacker has no pending rewards from pre-open redistributions), violating INVARIANT (new troves must not receive rewards from redistributions that occurred before they opened), causing scoped impact: verify this is correctly enforced; if snapshot is not set at open time, attacker could claim pre-open redistribution rewards? Local proof idea: Foundry unit test; redistribute; open new trove; assert getPendingBNBReward(newTrove) == 0."
]