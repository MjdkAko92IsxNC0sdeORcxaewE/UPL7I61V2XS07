[
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setUSDSGasCompensation / TroveManager._sendGasCompensation] Can an unprivileged liquidator under the precondition that USDSGasCompensation is raised via TimeLock after troves are opened (GasPool holds old_rate * N USDS) trigger openTrove(old_rate) -> TimeLock.schedule+execute(setUSDSGasCompensation(new_rate > old_rate)) -> liquidate(victim), violating the invariant that GasPool USDS balance >= sum of USDSGasCompensation recorded at each trove's open, causing scoped impact: liquidator receives new_rate USDS from GasPool per liquidation while only old_rate was deposited, draining GasPool beyond its funded balance? Local proof idea: add unit test deploying SystemState with USDSGasCompensation=10e18, open N troves, raise to 20e18 via mock timelock, liquidate all, assert GasPool.balanceOf(usdsToken) goes negative or returnFromPool reverts.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setUSDSGasCompensation / LiquityBase._getCompositeDebt] Can an unprivileged borrower under the precondition that USDSGasCompensation is set to 0 via TimeLock trigger TimeLock.execute(setUSDSGasCompensation(0)) -> openTrove(collateral, debt), violating the invariant that compositeDebt = netDebt + USDSGasCompensation > netDebt (so ICR is computed on a larger denominator), causing scoped impact: borrower opens a trove with ICR computed on netDebt alone (no gas comp buffer), allowing higher leverage than the protocol intends and bypassing the gas compensation solvency buffer? Local proof idea: add unit test setting USDSGasCompensation=0, open trove at exactly MCR collateral ratio using netDebt as denominator, assert ICR == MCR exactly (no gas comp buffer), compare with ICR when gas comp = 10e18.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setUSDSGasCompensation / BorrowerOperations._requireValidUSDSRepayment] Can an unprivileged borrower under the precondition that USDSGasCompensation is raised above the trove's current debt via TimeLock trigger openTrove(debt=minNetDebt) -> TimeLock.execute(setUSDSGasCompensation(debt + 1)) -> repayUSDS(amount), violating the invariant that _currentDebt.sub(USDS_GAS_COMPENSATION) >= 0 (SafeMath), causing scoped impact: repayUSDS reverts for all existing troves whose debt < new USDSGasCompensation, permanently locking borrowers out of partial repayment and forcing them into liquidation where the liquidator claims the inflated gas compensation? Local proof idea: fuzz test with USDSGasCompensation in [0, 2*troveDebt], assert repayUSDS never reverts with underflow when debt > 0.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setRedemptionFeeFloor / TroveManager._calcRedemptionRate] Can an unprivileged redeemer under the precondition that redemptionFeeFloor is set to 0 via TimeLock and baseRate has fully decayed to 0 trigger TimeLock.execute(setRedemptionFeeFloor(0)) -> wait BOOTSTRAP_PERIOD -> redeemCollateral(largeAmount, maxFeePercentage=0), violating the invariant that redemptionFee >= redemptionFeeFloor * BNBDrawn > 0, causing scoped impact: redeemer pays near-zero fee (only oracleRate component) to redeem USDS for BNB at face value, extracting BNB from the protocol at below-floor cost and bypassing the minimum fee protection for trove owners? Local proof idea: unit test with redemptionFeeFloor=0, baseRate=0, oracleRate=0 (Chainlink path), assert _calcRedemptionRate returns 0 and redeemer receives full BNB with zero fee deducted.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setBorrowingFeeFloor / TroveManager._calcBorrowingRate / SableStakingV2] Can an unprivileged borrower under the precondition that borrowingFeeFloor is set to 0 via TimeLock and baseRate has decayed to 0 trigger TimeLock.execute(setBorrowingFeeFloor(0)) -> openTrove(largeDebt, maxFeePercentage=0), violating the invariant that borrowingFee >= borrowingFeeFloor * USDSDebt > 0 (fee sent to SableStaking), causing scoped impact: borrower mints USDS with zero fee, SableStaking receives 0 USDS fee revenue, stakers lose their entitled fee reward share while borrower gains free USDS issuance? Local proof idea: unit test with borrowingFeeFloor=0, baseRate=0, oracleRate=0, assert _calcBorrowingFee returns 0 and SableStaking.increaseF_USDS is called with 0.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setCCR / LiquityBase._checkRecoveryMode] Can an unprivileged liquidator under the precondition that CCR is raised via TimeLock above the current TCR (forcing recovery mode) trigger TimeLock.execute(setCCR(newCCR > currentTCR)) -> liquidate(victim_ICR_between_MCR_and_CCR), violating the invariant that a trove with ICR >= MCR cannot be liquidated in normal mode, causing scoped impact: liquidator liquidates troves that were safe under normal mode rules (ICR >= MCR) by exploiting the recovery mode path, receiving collateral gas compensation and StabilityPool offset gains they were not entitled to? Local proof idea: unit test with TCR=140%, raise CCR to 145%, assert trove with ICR=120% (>MCR=110%) becomes liquidatable in recovery mode via _liquidateRecoveryMode.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setMCR / TroveManager._liquidateNormalMode] Can an unprivileged liquidator under the precondition that MCR is raised via TimeLock above the ICR of existing healthy troves trigger TimeLock.execute(setMCR(newMCR > victim_ICR)) -> liquidate(victim), violating the invariant that a trove with ICR >= old_MCR cannot be liquidated in normal mode, causing scoped impact: liquidator liquidates previously-safe troves by exploiting the retroactive MCR increase, receiving collateral gas compensation and StabilityPool BNB gains beyond their entitlement? Local proof idea: unit test with victim ICR=115%, raise MCR to 120% (still < CCR=150%), assert victim becomes liquidatable and liquidator receives collGasCompensation + USDSGasCompensation.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setMCR / TroveHelper.isValidFirstRedemptionHint] Can an unprivileged redeemer under the precondition that MCR is lowered via TimeLock trigger TimeLock.execute(setMCR(newMCR < old_MCR)) -> redeemCollateral(hint=trove_with_ICR_between_newMCR_and_oldMCR), violating the invariant that only troves with ICR >= MCR are eligible for redemption (first redemption hint check), causing scoped impact: redeemer targets troves that were previously ineligible for redemption (ICR < old_MCR) and extracts BNB at face value from those troves, bypassing the MCR eligibility gate? Local proof idea: unit test with trove ICR=108%, lower MCR to 105%, assert isValidFirstRedemptionHint returns true for that trove and redemption proceeds.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setUSDSGasCompensation / LiquityBase._getNetDebt / BorrowerOperations.closeTrove] Can an unprivileged borrower under the precondition that USDSGasCompensation is raised via TimeLock after trove opening trigger openTrove(gasComp=10e18) -> TimeLock.execute(setUSDSGasCompensation(20e18)) -> closeTrove(), violating the invariant that closeTrove burns exactly the gas compensation that was minted to GasPool at open time, causing scoped impact: closeTrove burns 20e18 from GasPool (new rate) but only 10e18 was deposited, over-burning GasPool balance and creating a USDS accounting deficit that benefits the borrower by reducing their effective debt repayment? Local proof idea: unit test open with gasComp=10e18, raise to 20e18, close trove, assert GasPool USDS balance goes negative or usdsToken.burn reverts.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setUSDSGasCompensation / BorrowerOperations.openTrove] Can an unprivileged borrower under the precondition that USDSGasCompensation is set to 0 via TimeLock trigger TimeLock.execute(setUSDSGasCompensation(0)) -> openTrove(debt=minNetDebt, collateral=minNetDebt*MCR/price), violating the invariant that GasPool receives USDSGasCompensation USDS per trove opened (so liquidators are compensated), causing scoped impact: borrower opens trove with 0 gas compensation minted to GasPool, liquidator of this trove receives 0 USDS gas compensation, while the borrower avoids the gas compensation debt overhead and gains a lower effective debt burden? Local proof idea: unit test with USDSGasCompensation=0, open trove, assert GasPool.balanceOf(usdsToken) unchanged and trove.debt == netDebt (no gas comp added).",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setBorrowingFeeFloor / BorrowerOperations._requireValidMaxFeePercentage] Can an unprivileged borrower under the precondition that borrowingFeeFloor is set to 0 via TimeLock trigger TimeLock.execute(setBorrowingFeeFloor(0)) -> openTrove(maxFeePercentage=0), violating the invariant that _requireValidMaxFeePercentage enforces maxFeePercentage >= borrowingFeeFloor > 0 in normal mode, causing scoped impact: borrower passes maxFeePercentage=0 in normal mode (since 0 >= 0), and if borrowingRate also computes to 0 (baseRate=0, oracleRate=0), the borrower mints USDS with zero fee, bypassing the minimum fee floor protection entirely? Local proof idea: unit test with borrowingFeeFloor=0, baseRate=0, oracleRate=0, call openTrove with maxFeePercentage=0, assert it does not revert and USDSBorrowingFeePaid event emits 0.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setMinNetDebt / BorrowerOperations._requireAtLeastMinNetDebt] Can an unprivileged borrower under the precondition that minNetDebt is set to 1 wei via TimeLock trigger TimeLock.execute(setMinNetDebt(1)) -> openTrove(netDebt=1, collateral=huge), violating the invariant that minNetDebt >= 90e18 prevents dust troves that distort TroveManager stake/redistribution math, causing scoped impact: attacker opens a dust trove with 1 wei debt and large collateral, inflating their stake relative to debt, receiving disproportionate redistribution rewards (L_BNB gains) from future liquidations compared to their debt contribution? Local proof idea: fuzz test with minNetDebt in [1, 90e18], open dust trove, trigger liquidation of another trove, assert L_BNB gain for dust trove is proportional to stake and does not exceed fair share.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setCCR / _setMCR ordering / setConfigs] Can an unprivileged deployer/owner under the precondition that setConfigs is called with _mcr and _ccr where _ccr is set first (when mcr=0) trigger setConfigs(timelock, _mcr=X, _ccr=Y) where Y > 0 but X >= Y, violating the invariant that MCR < CCR at all times, causing scoped impact: if _setMCR reverts (X >= Y), the transaction reverts entirely and ownership is not renounced, leaving the owner able to call setConfigs again with different parameters, potentially setting MCR/CCR to values that enable reward extraction before renouncing? Local proof idea: unit test calling setConfigs with _mcr=_ccr, assert revert, then call again with valid params, assert ownership renounced only after second call.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setCCR / _setMCR / TimeLock atomic update] Can an unprivileged TimeLock executor under the precondition that both MCR and CCR need to be raised (newMCR > oldCCR) trigger TimeLock.execute(setCCR(newCCR)) in one tx then TimeLock.execute(setMCR(newMCR)) in a second tx, violating the invariant that MCR < CCR is maintained atomically during the update window, causing scoped impact: between the two transactions, CCR is raised but MCR is still old, creating a window where TCR < newCCR triggers recovery mode with old MCR, allowing a liquidator to liquidate troves with ICR between oldMCR and newMCR that would not be liquidatable after the full update? Local proof idea: unit test simulating two-tx update with a liquidation sandwiched between them, assert liquidator gains BNB from troves that should be safe post-update.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setUSDSGasCompensation / TroveManager._liquidateNormalMode / _sendGasCompensation] Can an unprivileged liquidator under the precondition that USDSGasCompensation is raised to a value larger than GasPool's USDS balance trigger TimeLock.execute(setUSDSGasCompensation(largeValue)) -> batchLiquidateTroves([victim1, victim2, ...]), violating the invariant that totalUSDSGasCompensation sent to liquidator <= GasPool.balanceOf(usdsToken), causing scoped impact: _sendGasCompensation calls usdsToken.returnFromPool(gasPoolAddress, liquidator, totalUSDSGasCompensation) where totalUSDSGasCompensation > GasPool balance, either reverting (DoS) or if returnFromPool lacks balance check, minting excess USDS to the liquidator? Local proof idea: unit test with GasPool holding 10e18, raise USDSGasCompensation to 20e18, liquidate one trove, assert returnFromPool reverts or liquidator receives more than GasPool held.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setRedemptionFeeFloor / TroveManager.redeemCollateral / _requireUserAcceptsFee] Can an unprivileged redeemer under the precondition that redemptionFeeFloor is set to 0 and baseRate has decayed trigger TimeLock.execute(setRedemptionFeeFloor(0)) -> redeemCollateral(amount, maxFeePercentage=0, ...), violating the invariant that TroveHelper.requireValidMaxFeePercentage enforces maxFeePercentage >= redemptionFeeFloor > 0, causing scoped impact: redeemer passes maxFeePercentage=0 (since 0 >= 0), and with redemptionRate=0 the _requireUserAcceptsFee check passes, allowing redemption with zero fee and extracting BNB from trove owners at pure face value? Local proof idea: unit test with redemptionFeeFloor=0, baseRate=0, oracleRate=0, call redeemCollateral with maxFeePercentage=0, assert no revert and redemptionFee=0.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setUSDSGasCompensation / LiquityBase._getCompositeDebt / TroveManager.getCurrentICR] Can an unprivileged borrower under the precondition that USDSGasCompensation is raised via TimeLock after trove opening trigger openTrove(gasComp=10e18) -> TimeLock.execute(setUSDSGasCompensation(50e18)) -> getCurrentICR(borrower, price), violating the invariant that ICR is computed on the trove's stored debt (which includes old gas comp), causing scoped impact: ICR is now computed using stored debt (with old gas comp) but _getCompositeDebt uses new gas comp for new operations, creating an inconsistency where the trove appears more solvent than it is under the new parameter regime, allowing the borrower to withdraw collateral or borrow more than they should? Local proof idea: invariant test asserting getCurrentICR(trove) == collateral*price / trove.debt for all parameter values of USDSGasCompensation.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setCCR / LiquityBase._checkRecoveryMode / BorrowerOperations.withdrawColl] Can an unprivileged borrower under the precondition that CCR is lowered via TimeLock below the current TCR (exiting recovery mode) trigger TimeLock.execute(setCCR(newCCR < currentTCR)) -> withdrawColl(amount) in normal mode, violating the invariant that collateral withdrawal in normal mode only requires ICR >= MCR (not CCR), causing scoped impact: borrower exploits the CCR reduction to exit recovery mode and withdraw collateral that would have been blocked under the old CCR, extracting BNB from the protocol that was previously locked by the recovery mode constraint? Local proof idea: unit test with TCR=140%, CCR=150% (recovery mode), lower CCR to 130%, assert withdrawColl succeeds for a trove with ICR=135% that was previously blocked.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setMinNetDebt / BorrowerOperations.adjustTrove] Can an unprivileged borrower under the precondition that minNetDebt is raised via TimeLock above the debt of existing troves trigger openTrove(debt=100e18) -> TimeLock.execute(setMinNetDebt(200e18)) -> adjustTrove(withdrawUSDS=1), violating the invariant that existing troves below the new minNetDebt can still adjust (the check only applies to the resulting debt), causing scoped impact: if adjustTrove re-checks minNetDebt on the new resulting debt, a borrower with debt=100e18 cannot increase debt to 101e18 (< 200e18 new min), but can they withdraw collateral without changing debt, bypassing the minimum debt protection and leaving an undercollateralized dust position? Local proof idea: unit test raising minNetDebt above existing trove debt, attempt adjustTrove with collateral withdrawal only, assert whether minNetDebt check applies to collateral-only adjustments.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setUSDSGasCompensation / TroveManager._liquidateRecoveryMode] Can an unprivileged liquidator under the precondition that USDSGasCompensation is raised via TimeLock and a trove has ICR between 100% and MCR in recovery mode trigger TimeLock.execute(setUSDSGasCompensation(newValue)) -> liquidate(victim_ICR_100_to_MCR), violating the invariant that USDSGasCompensation paid to liquidator equals what was minted to GasPool at trove open, causing scoped impact: in _liquidateRecoveryMode the singleLiquidation.USDSGasCompensation is read live from systemState.getUSDSGasCompensation() (new value), but GasPool only holds old value per trove, so liquidator receives excess USDS gas compensation beyond what was funded? Local proof idea: unit test in recovery mode, open trove with gasComp=10e18, raise to 15e18, liquidate in recovery mode, assert liquidator USDS balance increases by 15e18 but GasPool only had 10e18.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setMCR / TroveHelper.isValidFirstRedemptionHint / TroveManager.redeemCollateral] Can an unprivileged redeemer under the precondition that MCR is lowered via TimeLock trigger TimeLock.execute(setMCR(newMCR)) -> redeemCollateral(amount, firstRedemptionHint=trove_with_ICR_between_newMCR_and_oldMCR), violating the invariant that redemption only targets troves with ICR >= MCR (protecting near-MCR troves from forced redemption), causing scoped impact: redeemer forces redemption against troves with ICR just above newMCR (previously protected), extracting BNB at face value from trove owners who had no reason to expect redemption at their collateral ratio? Local proof idea: unit test with trove ICR=109%, lower MCR from 110% to 105%, assert redeemCollateral with that trove as hint succeeds and trove owner loses collateral.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setBorrowingFeeFloor / TroveManager._calcBorrowingRate / ISableStakingV2.increaseF_USDS] Can an unprivileged borrower under the precondition that borrowingFeeFloor is set to 0 and baseRate=0 trigger TimeLock.execute(setBorrowingFeeFloor(0)) -> openTrove(largeDebt) -> SableStakingV2.increaseF_USDS(0), violating the invariant that each USDS minting event distributes a non-zero fee to SABLE_LP stakers proportional to debt minted, causing scoped impact: stakers who deposited SABLE_LP before the fee floor reduction receive 0 USDS fee reward for the borrowing event, while the borrower gains free USDS issuance, constituting unfair reward access where the borrower bypasses the fee that funds staker rewards? Local proof idea: unit test with borrowingFeeFloor=0, baseRate=0, open large trove, assert SableStakingV2.F_USDS unchanged and staker pending USDS reward = 0.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setUSDSGasCompensation / BorrowerOperations.openTrove / closeTrove] Can an unprivileged borrower under the precondition that USDSGasCompensation is lowered via TimeLock after trove opening trigger openTrove(gasComp=10e18) -> TimeLock.execute(setUSDSGasCompensation(5e18)) -> closeTrove(), violating the invariant that closeTrove burns exactly the gas compensation that was minted to GasPool at open time, causing scoped impact: closeTrove burns only 5e18 from GasPool (new lower rate) but 10e18 was deposited, leaving 5e18 USDS stranded in GasPool permanently, which future liquidators cannot claim (since new troves only deposit 5e18), creating a reward accounting surplus that no one can extract? Local proof idea: unit test open with gasComp=10e18, lower to 5e18, close trove, assert GasPool retains 5e18 excess USDS that is unclaimable.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setCCR / TroveManager._checkPotentialRecoveryMode / batchLiquidateTroves] Can an unprivileged liquidator under the precondition that CCR is raised via TimeLock mid-liquidation-sequence trigger TimeLock.execute(setCCR(newCCR)) -> batchLiquidateTroves([victim1, victim2]) where the sequence starts in normal mode but CCR change causes _checkPotentialRecoveryMode to return true mid-loop, violating the invariant that the liquidation mode (normal vs recovery) is determined once at the start of the sequence and does not change mid-batch, causing scoped impact: if the mode check is re-evaluated per trove in the batch, a liquidator could liquidate troves under recovery mode rules (lower ICR threshold) that were safe under normal mode rules? Local proof idea: unit test with CCR raised between batch entries, assert _getTotalsFromBatchLiquidate_RecoveryMode is not entered mid-batch for troves that were safe at batch start.",
  "[File: 'File Name: src/SystemState.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: _setUSDSGasCompensation / LiquityBase._getNetDebt / TroveManager.redeemCollateral] Can an unprivileged redeemer under the precondition that USDSGasCompensation is raised via TimeLock trigger TimeLock.execute(setUSDSGasCompensation(newValue)) -> redeemCollateral(amount), violating the invariant that _getNetDebt(trove.debt) correctly recovers the net debt for redemption accounting (trove.debt - gasComp), causing scoped impact: if gasComp is raised above the stored trove.debt, _getNetDebt underflows (SafeMath revert), blocking all redemptions and forcing trove owners to remain in the system while the redeemer cannot extract BNB, but also preventing the trove owner from closing their position? Local proof idea: fuzz test with USDSGasCompensation in [0, 2*troveDebt], assert redeemCollateral never reverts with underflow for any valid trove."
]