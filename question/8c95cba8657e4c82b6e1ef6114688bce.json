[
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: onlyRole modifier / execute] Can an unprivileged attacker who holds no role call TimeLock.execute() under the precondition that EXECUTOR_ROLE has been granted to address(0) via AccessControl.grantRole(EXECUTOR_ROLE, address(0)) by the TIMELOCK_ADMIN_ROLE holder, triggering execute(target=SystemState, value=0, data=abi.encodeCall(SystemState.setMCR, [1000000000000000001]), predecessor=bytes32(0), salt=0) after the delay, violating the invariant that only authorized executors may trigger SystemState parameter changes, causing scoped impact: attacker with no role sets MCR to minimum (just above 100%), enabling any borrower to open a trove at 100.0001% ICR and extract near-maximum USDS per BNB deposited? Local proof idea: add Foundry unit test deploying TimeLock with empty proposers/executors arrays, grant EXECUTOR_ROLE to address(0) from deployer, schedule setMCR(1000000000000000001) with minDelay, warp past delay, call execute() from address(1), assert SystemState.getMCR() == 1000000000000000001 and borrower can open trove at ICR just above 1e18.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: onlyRole modifier / schedule] Can an unprivileged attacker who holds no role call TimeLock.schedule() under the precondition that PROPOSER_ROLE has been granted to address(0) via AccessControl.grantRole(PROPOSER_ROLE, address(0)), triggering schedule(target=SystemState, value=0, data=abi.encodeCall(SystemState.setCCR, [1100000000000000002]), predecessor=bytes32(0), salt=0, delay=minDelay), violating the invariant that only authorized proposers may queue SystemState parameter changes, causing scoped impact: attacker queues CCR reduction to just above MCR, eliminating recovery mode protection and allowing borrowers to extract USDS at near-100% collateral ratio without triggering recovery mode? Local proof idea: add Foundry unit test granting PROPOSER_ROLE to address(0), call schedule() from address(1), assert operation is pending, warp past delay, execute, assert SystemState.getCCR() == 1100000000000000002.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: onlyRole modifier / execute + schedule (both open)] Can an unprivileged attacker call both schedule() and execute() under the precondition that both PROPOSER_ROLE and EXECUTOR_ROLE are granted to address(0), triggering schedule(SystemState, 0, setMCR(1000000000000000001), bytes32(0), salt1, minDelay) then warp then execute(), violating the invariant that the timelock role separation prevents unprivileged parameter changes, causing scoped impact: attacker with no role atomically queues and executes MCR reduction to minimum, then opens a trove at 100.0001% ICR minting maximum USDS from minimum BNB collateral? Local proof idea: add Foundry fuzz test with arbitrary attacker address, grant both roles to address(0), fuzz over MCR values in range (1e18, CCR), assert attacker can schedule+execute any valid MCR and subsequently borrow at that ratio.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: updateDelay / execute] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE reduce the timelock delay to zero under the precondition that minDelay > 0 initially, by scheduling execute(address(timeLock), 0, abi.encodeCall(TimeLock.updateDelay, [0]), bytes32(0), salt, currentMinDelay), waiting currentMinDelay, executing it, then immediately scheduling and executing SystemState.setMCR(1000000000000000001) with delay=0 in the same block, violating the invariant that the timelock delay provides a minimum reaction window before parameter changes take effect, causing scoped impact: attacker collapses the timelock delay to zero and instantly sets MCR to minimum, enabling immediate undercollateralized borrowing with no user exit window? Local proof idea: add Foundry unit test with proposer+executor roles, schedule updateDelay(0), warp, execute, then in same block schedule+execute setMCR(1000000000000000001) with delay=0, assert both succeed and MCR is at minimum.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: executeBatch / _call ordering before _afterCall] Can an attacker with EXECUTOR_ROLE re-execute a batch operation under the precondition that a scheduled batch targets a malicious contract as targets[0] and the batch is ready, by having the malicious contract's fallback re-enter executeBatch() with the same (targets, values, datas, predecessor, salt) parameters during _call() before _afterCall() sets _timestamps[id] = _DONE_TIMESTAMP, violating the invariant that each scheduled operation executes exactly once, causing scoped impact: the batch executes twice, and if the batch contains a BNB transfer (value[0] > 0) to the attacker, the attacker receives double the intended BNB from the TimeLock's balance? Local proof idea: add Foundry unit test with TimeLock holding BNB, schedule a batch with targets[0]=ReentrantReceiver that re-enters executeBatch with same params, assert TimeLock BNB balance decreases by 2x value[0] after execution.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / _call ordering before _afterCall] Can an attacker with EXECUTOR_ROLE re-execute a single operation under the precondition that a scheduled operation targets a malicious contract and is ready, by having the malicious contract's receive() re-enter execute() with the same (target, value, data, predecessor, salt) before _afterCall() marks the id as done, violating the invariant that _timestamps[id] == _DONE_TIMESTAMP prevents re-execution, causing scoped impact: the operation executes twice before _afterCall fires, and if value > 0 the attacker drains double the BNB from the TimeLock's native balance? Local proof idea: add Foundry unit test, fund TimeLock with 2 ether, schedule execute(ReentrantReceiver, 1 ether, '', bytes32(0), salt, minDelay), warp, call execute(), assert ReentrantReceiver received 2 ether and TimeLock balance is 0.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / receive() + value parameter] Can an attacker with EXECUTOR_ROLE drain BNB held by the TimeLock under the precondition that the TimeLock holds a nonzero BNB balance (TimeLock has receive() payable), by scheduling execute(target=attacker, value=timelockBNBBalance, data='', predecessor=bytes32(0), salt=0, delay=minDelay) and executing after the delay, violating the invariant that BNB held by the TimeLock is only used for legitimate maintenance operations and not extractable by an executor to an arbitrary address, causing scoped impact: attacker with EXECUTOR_ROLE drains the entire BNB balance of the TimeLock to an attacker-controlled address? Local proof idea: add Foundry unit test, send 10 ether to TimeLock via receive(), schedule execute(attacker, 10 ether, '', bytes32(0), 0, minDelay), warp, execute, assert attacker.balance increased by 10 ether and TimeLock.balance == 0.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: executeBatch / value array + receive()] Can an attacker with EXECUTOR_ROLE drain BNB from the TimeLock via a batch under the precondition that the TimeLock holds BNB and a batch is scheduled with values[0] = timelockBNBBalance and targets[0] = attacker, by calling executeBatch() after the delay with msg.value=0 but relying on the TimeLock's own BNB balance to fund the call, violating the invariant that the TimeLock's BNB balance is not extractable to arbitrary addresses via batch execution, causing scoped impact: attacker drains TimeLock BNB balance to attacker-controlled address via batch? Local proof idea: add Foundry unit test, fund TimeLock, schedule batch with targets=[attacker], values=[timelockBalance], datas=[''], warp, executeBatch, assert attacker received full balance.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: executeBatch / empty arrays + predecessor bypass] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE mark an operation as done without executing any calls under the precondition that executeBatch accepts empty arrays (targets.length == 0 passes length-mismatch checks trivially), by scheduling executeBatch([], [], [], bytes32(0), salt, minDelay) and executing it to mark the resulting id as _DONE_TIMESTAMP, then using that id as the predecessor for a second operation that calls SystemState.setMCR(minimum), violating the invariant that a predecessor operation must have performed meaningful protocol actions before unlocking dependent operations, causing scoped impact: attacker bypasses predecessor dependency semantics to unlock and execute a SystemState MCR reduction that would otherwise require a real prior operation? Local proof idea: add Foundry unit test, schedule+execute empty batch, capture its id, schedule setMCR(1000000000000000001) with predecessor=emptyBatchId, warp, execute, assert MCR is at minimum.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / grantRole(EXECUTOR_ROLE, address(0)) via TimeLock self-call] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE permanently open the EXECUTOR_ROLE to all addresses under the precondition that the TimeLock has TIMELOCK_ADMIN_ROLE over itself (set in constructor), by scheduling execute(address(timeLock), 0, abi.encodeCall(AccessControl.grantRole, [EXECUTOR_ROLE, address(0)]), bytes32(0), salt, minDelay) and executing after the delay, violating the invariant that EXECUTOR_ROLE is restricted to authorized addresses and cannot be opened to the public, causing scoped impact: after execution any address can call execute() to run arbitrary SystemState parameter changes, enabling permanent unprivileged fund extraction via MCR/CCR manipulation? Local proof idea: add Foundry unit test, schedule+execute grantRole(EXECUTOR_ROLE, address(0)) via TimeLock self-call, then call execute() from address(999) with no role, assert it succeeds.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / grantRole(PROPOSER_ROLE, address(0)) via TimeLock self-call] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE permanently open the PROPOSER_ROLE to all addresses under the precondition that the TimeLock has TIMELOCK_ADMIN_ROLE over itself, by scheduling execute(address(timeLock), 0, abi.encodeCall(AccessControl.grantRole, [PROPOSER_ROLE, address(0)]), bytes32(0), salt, minDelay) and executing after the delay, violating the invariant that PROPOSER_ROLE is restricted to authorized addresses, causing scoped impact: after execution any address can call schedule() to queue arbitrary SystemState parameter changes, and if EXECUTOR_ROLE is also open, any address can schedule+execute MCR/CCR reductions to extract USDS? Local proof idea: add Foundry unit test, schedule+execute grantRole(PROPOSER_ROLE, address(0)) via self-call, then call schedule() from address(999), assert it succeeds.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: executeBatch / atomic MCR+CCR reduction] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE atomically reduce both MCR and CCR to their minimum values under the precondition that _setMCR requires value < ccr and _setCCR requires value > mcr, by scheduling a batch with targets=[SystemState, SystemState], datas=[setCCR(1100000000000000002), setMCR(1000000000000000001)] (CCR first to satisfy ordering constraint), executing after the delay, violating the invariant that MCR >= 110% and CCR >= 150% as protocol safety parameters, causing scoped impact: attacker sets MCR to just above 100% and CCR to just above MCR, enabling borrowers to open troves at 100.0001% ICR and extract near-maximum USDS per BNB with no recovery mode protection? Local proof idea: add Foundry unit test, schedule batch [setCCR(1100000000000000002), setMCR(1000000000000000001)], warp, executeBatch, assert MCR == 1000000000000000001 and CCR == 1100000000000000002, then open trove at ICR just above MCR and assert USDS minted.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: executeBatch / atomic updateDelay(0) + setMCR(min) in same batch] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE collapse the timelock delay and set MCR to minimum in a single atomic batch under the precondition that the current minDelay > 0, by scheduling a batch with targets=[timeLock, SystemState], datas=[updateDelay(0), setMCR(1000000000000000001)], waiting the current minDelay, executing the batch, violating the invariant that the timelock delay and MCR are independently protected parameters, causing scoped impact: attacker atomically eliminates the timelock delay and sets MCR to minimum in one transaction, then immediately schedules and executes further parameter changes with zero delay, enabling instant undercollateralized borrowing? Local proof idea: add Foundry unit test, schedule batch [updateDelay(0), setMCR(1000000000000000001)], warp by original minDelay, executeBatch, assert minDelay == 0 and MCR == 1000000000000000001, then schedule+execute another change with delay=0 in same block.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setMCR cross-module → BorrowerOperations.openTrove] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE extract excess USDS by reducing MCR via TimeLock then borrowing under the precondition that MCR is currently 110% and the attacker has BNB collateral, by scheduling+executing setMCR(1000000000000000001) via TimeLock, then calling BorrowerOperations.openTrove() with ICR just above 1000000000000000001 (just above 100%), violating the invariant that every USDS mint corresponds to collateral at >= 110% ICR, causing scoped impact: attacker mints USDS at near-100% collateral ratio, extracting ~9% more USDS per BNB than the protocol intends, creating undercollateralized debt? Local proof idea: add Foundry integration test, reduce MCR to 1000000000000000001 via TimeLock, open trove with 1 BNB at price $700 and borrow ~699 USDS (ICR ~100.1%), assert USDS minted and trove ICR < original MCR of 110%.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setCCR cross-module → recovery mode suppression] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE suppress recovery mode by reducing CCR via TimeLock under the precondition that the current TCR is between the new CCR and the old CCR, by scheduling+executing setCCR(newCCR) where newCCR < currentTCR < oldCCR, violating the invariant that recovery mode triggers when TCR < CCR and protects the system from undercollateralization, causing scoped impact: attacker prevents recovery mode from activating, allowing borrowers to withdraw collateral or increase debt in a system that should be in recovery mode, extracting BNB or USDS that would otherwise be blocked? Local proof idea: add Foundry integration test, set up system with TCR between 110% and 150%, reduce CCR to just below current TCR via TimeLock, assert checkRecoveryMode() returns false, then call withdrawColl() that would have been blocked in recovery mode.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setRedemptionFeeFloor(0) cross-module → TroveManager.redeemCollateral] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE extract BNB at below-market cost by reducing the redemption fee floor to zero via TimeLock under the precondition that baseRate has decayed to near-zero and oracleRate is near-zero, by scheduling+executing setRedemptionFeeFloor(0) via TimeLock, then calling TroveManager.redeemCollateral() when baseRate ≈ 0 and oracleRate ≈ 0, violating the invariant that redemptionFeeFloor provides a minimum fee that prevents zero-cost redemption, causing scoped impact: attacker redeems USDS for BNB with near-zero fee, extracting BNB at face value without paying the protocol's minimum redemption cost? Local proof idea: add Foundry integration test, set redemptionFeeFloor to 0 via TimeLock, let baseRate decay to 0, call redeemCollateral with oracleRate=0, assert BNBFee == 0 and attacker receives full BNB value.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setBorrowingFeeFloor(0) cross-module → BorrowerOperations.openTrove] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE borrow USDS with zero fee by reducing the borrowing fee floor to zero via TimeLock under the precondition that baseRate has decayed to near-zero and oracleRate is near-zero, by scheduling+executing setBorrowingFeeFloor(0) via TimeLock, then calling BorrowerOperations.openTrove() when baseRate ≈ 0 and oracleRate ≈ 0, violating the invariant that borrowingFeeFloor provides a minimum fee that prevents zero-cost USDS minting, causing scoped impact: attacker borrows USDS with zero fee, extracting USDS without paying the protocol's minimum borrowing cost, reducing SableStakingV2 fee revenue? Local proof idea: add Foundry integration test, set borrowingFeeFloor to 0 via TimeLock, let baseRate decay to 0, open trove with oracleRate=0, assert USDSFee == 0 and borrower receives full requested USDS amount.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setMinNetDebt(1) cross-module → gas compensation extraction] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE extract USDS gas compensation by reducing minNetDebt to 1 wei via TimeLock under the precondition that USDSGasCompensation is 10e18, by scheduling+executing setMinNetDebt(1) via TimeLock, then opening many dust troves with netDebt=1 wei (each minting 10e18 USDS to GasPool), then having a liquidator liquidate them to claim the 10e18 USDS gas compensation per trove, violating the invariant that minNetDebt prevents dust troves that create disproportionate gas compensation relative to debt, causing scoped impact: attacker opens N dust troves each minting 10e18 USDS to GasPool, then liquidates them to extract N * 10e18 USDS as gas compensation while depositing only N * 1 wei net debt? Local proof idea: add Foundry integration test, set minNetDebt to 1 via TimeLock, open 10 dust troves with 1 wei netDebt, drop price to trigger liquidation, assert liquidator receives 10 * 10e18 USDS gas compensation.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setUSDSGasCompensation cross-module → GasPool accounting mismatch] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE create a GasPool accounting mismatch by changing USDSGasCompensation after troves are opened under the precondition that existing troves have gas compensation locked at the old value, by scheduling+executing setUSDSGasCompensation(newValue) via TimeLock where newValue != oldValue, then closing or liquidating existing troves which burn newValue USDS from GasPool instead of oldValue, violating the invariant that GasPool USDS balance equals sum of all open trove gas compensations, causing scoped impact: if newValue < oldValue, excess USDS is stranded in GasPool; if newValue > oldValue, GasPool is drained faster than funded, creating an accounting deficit that breaks the USDS supply invariant? Local proof idea: add Foundry integration test, open trove with gasComp=10e18, change USDSGasCompensation to 5e18 via TimeLock, close trove, assert GasPool has 5e18 stranded USDS and USDS.totalSupply != ActivePool.USDSDebt + DefaultPool.USDSDebt.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setUSDSGasCompensation(0) cross-module → _requireValidUSDSRepayment bypass] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE bypass the repayment cap by setting USDSGasCompensation to 0 via TimeLock under the precondition that _requireValidUSDSRepayment checks _debtRepayment <= _currentDebt.sub(USDS_GAS_COMPENSATION), by scheduling+executing setUSDSGasCompensation(0) via TimeLock, then calling repayUSDS() with _debtRepayment == _currentDebt (full debt including gas compensation portion), violating the invariant that the gas compensation portion of debt is never repayable by the borrower directly, causing scoped impact: attacker repays the full composite debt including the gas compensation component, leaving GasPool holding USDS that was never burned, breaking the USDS supply invariant? Local proof idea: add Foundry integration test, open trove, set USDSGasCompensation to 0 via TimeLock, call repayUSDS with full compositeDebt, assert GasPool still holds original 10e18 USDS and USDS.totalSupply > ActivePool.USDSDebt.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setMCR cross-module → existing troves become liquidatable] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE force existing troves into liquidatable state by increasing MCR above their current ICR via TimeLock under the precondition that existing troves have ICR between old MCR and new MCR, by scheduling+executing setMCR(newMCR) where newMCR > existingTroveICR, then calling TroveManager.liquidate() on those troves, violating the invariant that troves with ICR >= original MCR should not be liquidatable without a price change, causing scoped impact: attacker liquidates previously-healthy troves, claiming gas compensation and triggering StabilityPool offsets that redistribute BNB gains to the attacker as a StabilityPool depositor? Local proof idea: add Foundry integration test, open trove at 115% ICR, increase MCR to 120% via TimeLock, call liquidate(), assert trove is liquidated and attacker receives gas compensation.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setCCR cross-module → force recovery mode on existing system] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE force the system into recovery mode by increasing CCR above the current TCR via TimeLock under the precondition that the current TCR is between old CCR and new CCR, by scheduling+executing setCCR(newCCR) where newCCR > currentTCR, then calling TroveManager.liquidateTroves() in recovery mode which liquidates troves with ICR < CCR (not just MCR), violating the invariant that recovery mode is only triggered by genuine undercollateralization, causing scoped impact: attacker forces recovery mode, enabling liquidation of troves with ICR between MCR and CCR that would be safe in normal mode, extracting gas compensation and BNB gains? Local proof idea: add Foundry integration test, set up system with TCR=130%, increase CCR to 140% via TimeLock, assert checkRecoveryMode() returns true, liquidate trove with ICR=125% that was previously safe.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: executeBatch / atomic setBorrowingFeeFloor(0) + setRedemptionFeeFloor(0)] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE atomically eliminate both borrowing and redemption fees via a single batch under the precondition that both fee floors are currently nonzero, by scheduling a batch with targets=[SystemState, SystemState], datas=[setBorrowingFeeFloor(0), setRedemptionFeeFloor(0)], executing after the delay, violating the invariant that fee floors provide minimum protocol revenue and cannot both be zero simultaneously, causing scoped impact: attacker borrows USDS with zero fee and redeems BNB with zero fee in the same block, extracting the full spread between borrow and redemption without paying any protocol fees? Local proof idea: add Foundry integration test, execute batch setting both floors to 0, let baseRate decay to 0, borrow USDS with zero fee then redeem for BNB with zero fee, assert net profit > 0 with no fees paid.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: updateDelay / zero delay → same-block schedule+execute] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE schedule and execute a SystemState change in the same block after setting delay to zero under the precondition that _schedule sets _timestamps[id] = block.timestamp + 0 = block.timestamp and isOperationReady checks timestamp <= block.timestamp, by first executing updateDelay(0) via TimeLock, then in the same block calling schedule() and immediately execute() for setMCR(minimum), violating the invariant that the timelock delay provides a nonzero window between scheduling and execution, causing scoped impact: attacker collapses the timelock to a no-op, enabling instant MCR/CCR/fee changes with no user reaction window, then immediately borrows at the new parameters? Local proof idea: add Foundry unit test, set delay to 0, in same block call schedule(setMCR(1000000000000000001), delay=0) then execute(), assert both succeed in same block.timestamp.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / _call reentrancy + _afterCall state ordering] Can an attacker with EXECUTOR_ROLE exploit the state ordering in execute() where _call() fires before _afterCall() sets _timestamps[id] = _DONE_TIMESTAMP under the precondition that the target contract re-enters execute() with the same parameters and catches the inner revert, by deploying a ReentrantTarget that in its fallback calls execute() with the same (target, value, data, predecessor, salt), catches the inner revert via try/catch, and returns normally, violating the invariant that _timestamps[id] == _DONE_TIMESTAMP prevents any re-execution of the same operation id, causing scoped impact: the outer execute() completes successfully after the inner re-entrant execute() fails, but the operation's side effects (e.g., SystemState parameter change) have already been applied once; if the target is a SystemState setter, the parameter is set correctly but the reentrancy window could be used to front-run the state change? Local proof idea: add Foundry unit test with ReentrantTarget that re-enters execute() and catches revert, assert _timestamps[id] == _DONE_TIMESTAMP after outer execute() and that the operation executed exactly once."
]