[
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> SableStakingV2.setAddresses() re-initialization] Can an attacker with EXECUTOR_ROLE schedule and execute a call to SableStakingV2.setAddresses() (if ownership was not renounced) to replace troveManagerAddress or borrowerOperationsAddress with attacker-controlled contracts, then call increaseF_BNB or increaseF_USDS from those fake contracts to inflate the F_BNB/F_USDS accumulators, violating the invariant that fee accumulators must only increase via genuine protocol fee events, causing scoped impact: attacker inflates F_BNB before staking, stakes LP, then unstakes to claim BNB that was never actually collected as protocol fees? Local proof idea: if SableStakingV2 owner == TimeLock, schedule setAddresses with fake TroveManager; execute; call fake TM's increaseF_BNB(1e30); stake 1 LP; unstake; assert BNBGain = 1e30 * 1 / totalStaked.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: isOperationReady() / block.timestamp boundary] Can an attacker with EXECUTOR_ROLE call execute() at exactly block.timestamp == _timestamps[id] (the boundary condition where isOperationReady returns true since timestamp <= block.timestamp), bypassing any intended off-by-one protection, and in the same block also call provideToSP to deposit into StabilityPool before the parameter change takes effect in the same block's state, violating the invariant that the execution block must not allow simultaneous reward-position manipulation and governance execution, causing scoped impact: attacker executes MCR change and deposits into SP in the same block, capturing liquidation gains from troves that became eligible in that exact block? Local proof idea: warp to exactly _timestamps[id]; in same block call execute() then provideToSP; assert both succeed; assert attacker captures BNB gain from same-block liquidation.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: TIMELOCK_ADMIN_ROLE self-administration / role escalation] Can an attacker who gains TIMELOCK_ADMIN_ROLE (which is granted to the TimeLock contract itself at construction) schedule and execute a call to TimeLock.grantRole(EXECUTOR_ROLE, attacker_address), using the TimeLock's self-administration capability to grant themselves EXECUTOR_ROLE without any external multisig approval, violating the invariant that role grants must require external governance authorization, causing scoped impact: attacker self-grants EXECUTOR_ROLE and then executes any pending or newly scheduled SystemState parameter change to manipulate reward distribution? Local proof idea: schedule grantRole(EXECUTOR_ROLE, attacker) targeting TimeLock itself; warp past minDelay; execute; assert attacker hasRole(EXECUTOR_ROLE); then execute any pending setMCR operation; assert MCR changed.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> AccessControl.grantRole(PROPOSER_ROLE, address(0))] Can an attacker with EXECUTOR_ROLE schedule and execute a call to TimeLock.grantRole(PROPOSER_ROLE, address(0)), making the PROPOSER_ROLE open to everyone, then any unprivileged user can schedule arbitrary SystemState parameter changes, violating the invariant that proposal rights must be restricted to authorized governance participants, causing scoped impact: any attacker can schedule setMCR(lower) and after minDelay execute it (if EXECUTOR_ROLE is also open), enabling permissionless manipulation of liquidation thresholds and reward distribution? Local proof idea: schedule grantRole(PROPOSER_ROLE, address(0)) targeting TimeLock; execute; assert hasRole(PROPOSER_ROLE, address(0)) == true; call schedule(setMCR) from random EOA; assert succeeds; warp; execute; assert MCR changed.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> AccessControl.revokeRole(PROPOSER_ROLE, legitimate_proposer)] Can an attacker with TIMELOCK_ADMIN_ROLE schedule and execute a call to TimeLock.revokeRole(PROPOSER_ROLE, legitimate_multisig), removing the only authorized proposer, then schedule their own operations as the new sole proposer (if they also hold PROPOSER_ROLE), violating the invariant that governance role revocation must not enable a single party to monopolize proposal rights and manipulate reward parameters, causing scoped impact: attacker becomes sole proposer, schedules favorable MCR/fee changes, and extracts excess StabilityPool or staking rewards? Local proof idea: schedule revokeRole(PROPOSER_ROLE, multisig); execute; assert multisig cannot schedule; assert attacker (with PROPOSER_ROLE) is sole proposer; schedule+execute setRedemptionFeeFloor(0); redeem; assert zero fee to stakers.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: updateDelay() / self-call only restriction] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE schedule a call to TimeLock.updateDelay(0) (target = address(this)), wait minDelay, execute it to set _minDelay = 0, then in the same block schedule and execute SystemState.setMCR(1.001e18) with delay=0, violating the invariant that all SystemState mutations must be preceded by a non-zero community review window, causing scoped impact: attacker atomically removes the timelock protection and executes a reward-affecting parameter change with no delay, denying other users the exit window? Local proof idea: schedule updateDelay(0); warp minDelay; execute; assert _minDelay == 0; schedule setMCR(1.001e18) with delay=0; assert _timestamps[id] == block.timestamp; execute in same block; assert MCR changed with zero delay.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: executeBatch() / _afterCall called once for entire batch] Can an attacker with EXECUTOR_ROLE schedule a batch where one of the calls in the batch fails (target reverts) but _call() requires success, causing the entire executeBatch to revert, but if the attacker crafts the batch so that a successful partial execution leaves intermediate state changes (impossible with revert, but testable), or alternatively, can the attacker use a batch where all calls succeed but the combined effect of multiple SystemState changes in one batch bypasses individual parameter validation ordering (e.g., setCCR then setMCR where MCR < CCR check depends on the new CCR), violating the invariant that batch parameter changes must satisfy all individual constraints at each step, causing scoped impact: attacker achieves MCR/CCR combination that would fail if applied individually, enabling undercollateralized borrowing or excess liquidation? Local proof idea: scheduleBatch([setCCR(1.12e18), setMCR(1.11e18)]); execute; assert both applied; assert MCR=1.11e18 < CCR=1.12e18 valid; assert troves at ICR 1.11-1.15e18 now liquidatable.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: schedule() / delay >= getMinDelay() check / minDelay = 0 edge case] Can an attacker with PROPOSER_ROLE, after _minDelay has been set to 0 via updateDelay(0), call schedule() with delay=0, causing _timestamps[id] = SafeMath.add(block.timestamp, 0) = block.timestamp, and then immediately call execute() in the same transaction (since isOperationReady checks timestamp <= block.timestamp which is true), violating the invariant that schedule and execute must be separate transactions with a non-zero time gap, causing scoped impact: attacker schedules and executes a SystemState parameter change atomically in one block, with no window for other users to observe the CallScheduled event and exit their positions before the change affects reward distribution? Local proof idea: set _minDelay=0; in single tx: call schedule(setMCR, delay=0); call execute(setMCR); assert both succeed in same block; assert MCR changed with zero observable delay.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> CommunityIssuance.transferOwnership()] Can an attacker with EXECUTOR_ROLE (assuming TimeLock is owner of CommunityIssuance via OwnableTransfer) schedule and execute CommunityIssuance.transferOwnership(attacker_address), transferring ownership of CommunityIssuance to the attacker, who then calls updateRewardPerSec(0) to halt SABLE issuance or updateRewardPerSec(very_large) to drain the SABLE token balance, violating the invariant that CommunityIssuance ownership must remain with the TimeLock governance system, causing scoped impact: attacker drains SABLE from CommunityIssuance by setting an astronomically high reward rate, causing StabilityPool depositors to claim SABLE far beyond the intended issuance schedule? Local proof idea: schedule transferOwnership(attacker) on CommunityIssuance; execute; assert CommunityIssuance.owner() == attacker; call updateRewardPerSec(1e30) from attacker; warp 1 block; triggerSABLEIssuance; assert totalSABLEIssued >> intended; assert SABLE balance of CommunityIssuance drained.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() / no validation of target contract code] Can an attacker with EXECUTOR_ROLE schedule an operation where target is an EOA (no code), causing _call() to succeed (EVM calls to EOAs with any data succeed and return true), and _afterCall() to mark the operation as done, while the intended SystemState parameter change never actually executed, violating the invariant that executed operations must have actually called the intended target contract, causing scoped impact: a critical protective parameter change (e.g., MCR increase) is marked as done but never applied, leaving the system in an unsafe state where the attacker can continue extracting liquidation rewards under the old parameters? Local proof idea: schedule op targeting EOA with setMCR calldata; execute; assert _timestamps[id] == _DONE_TIMESTAMP; assert MCR unchanged; assert op cannot be re-scheduled (already done); assert attacker continues to benefit from old MCR.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: schedule() + execute() / front-running with StabilityPool epoch boundary] Can an attacker observe a pending setMCR(lower) operation and time their StabilityPool deposit to occur just before a pool-emptying liquidation (epoch increment) that will be triggered by the MCR change, ensuring their deposit snapshot captures the new epoch's G accumulator from zero, while existing depositors who survived the epoch change have their deposits zeroed, violating the invariant that epoch transitions must not allow new depositors to claim SABLE gains that pre-date their deposit, causing scoped impact: attacker deposits after epoch reset, captures all subsequent SABLE issuance with a fresh G snapshot while old depositors lost their deposits? Local proof idea: trigger epoch reset via liquidation; immediately provideToSP; assert depositSnapshots[attacker].epoch == currentEpoch; assert old depositors' compoundedDeposit == 0; assert attacker captures 100% of next SABLE issuance.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> SystemState parameter change / SableStakingV2 snapshot not updated] Can an attacker with EXECUTOR_ROLE execute a SystemState.setBorrowingFeeFloor(high_value) change, causing all subsequent borrowing operations to generate large USDS fees sent to SableStakingV2.increaseF_USDS, while the attacker had pre-staked LP tokens before the change (with a low F_USDS snapshot), allowing them to claim the entire fee accumulation delta since their stake, violating the invariant that staking rewards must be proportional to stake duration under the actual fee rate, causing scoped impact: attacker captures USDS fee rewards that were generated by a fee rate they artificially inflated via governance? Local proof idea: stake LP; record F_USDS_Snapshot; schedule+execute setBorrowingFeeFloor(5e16); open 100 troves; assert F_USDS increased by large amount; unstake; assert USDSGain = stake * (F_USDS - F_USDS_Snapshot) / DECIMAL_PRECISION.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() / no return value check from _call()] Can an attacker with EXECUTOR_ROLE schedule an operation targeting a contract that returns false (non-reverting failure) from the called function, where _call() only checks the low-level call success (bool success from .call{value}(data)) but not any return value, causing the operation to be marked done even though the target function indicated failure, violating the invariant that executed operations must have successfully applied their intended state change, causing scoped impact: a SystemState parameter update that returns false is marked done and cannot be retried, leaving reward parameters in an unintended state that benefits the attacker? Local proof idea: deploy MockSystemState that returns false from setMCR without reverting; schedule op; execute; assert _timestamps[id] == _DONE_TIMESTAMP; assert MockSystemState.mcr unchanged; assert op cannot be re-scheduled.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: execute() -> StabilityPool.setParams() re-initialization] Can an attacker with EXECUTOR_ROLE (if TimeLock is owner of StabilityPool) schedule and execute StabilityPool.setParams() to replace communityIssuance with an attacker-controlled contract that returns arbitrary SABLE issuance amounts, violating the invariant that SABLE issuance to StabilityPool must come from the legitimate CommunityIssuance contract with its rate-limited schedule, causing scoped impact: attacker's fake CommunityIssuance returns type(uint256).max from issueSABLE(), inflating the G accumulator, allowing the attacker to claim unlimited SABLE from the real CommunityIssuance.sendSABLE() call? Local proof idea: deploy FakeCommunityIssuance returning 1e50 from issueSABLE; schedule setParams with fake CI address; execute; call provideToSP; assert G accumulator inflated; assert getDepositorSABLEGain(attacker) == 1e50 * deposit / P; assert sendSABLE drains real SABLE balance."
]