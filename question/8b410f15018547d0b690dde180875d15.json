[
  "[File: 'File Name: src/ActivePool.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: StabilityPool._getSABLEGainFromSnapshots / epochToScaleToG / scale boundary SABLE gain] Can an unprivileged ATTACKER (StabilityPool depositor) under PRECONDITIONS (currentScale is about to increment, attacker has a deposit with scaleSnapshot == currentScale - 1, i.e., spanning two scale changes) trigger CALL_SEQUENCE (attacker's deposit spans two scale changes -> _getCompoundedStakeFromSnapshots returns 0 (scaleDiff >= 2) -> but _getSABLEGainFromSnapshots still computes firstPortion = epochToScaleToG[epoch][scaleSnapshot] - G_snapshot and secondPortion = epochToScaleToG[epoch][scaleSnapshot+1] / SCALE_FACTOR -> attacker calls withdrawFromSP and receives SABLE gain), violating INVARIANT (if compounded deposit is 0 due to spanning two scale changes, the SABLE gain should also be 0 or negligible; the SABLE gain formula uses initialStake not compoundedStake, so it may return non-zero SABLE even when the deposit is effectively 0), causing scoped impact: attacker receives SABLE gains computed on their full initialStake even though their effective deposit has been reduced to near-zero by two scale changes, extracting excess SABLE from CommunityIssuance? Local proof idea: write a unit test with two scale changes; assert SABLE gain for a deposit spanning both scale changes is 0 or proportional to the near-zero compounded deposit.",
  "[File: 'File Name: src/ActivePool.sol -> Scope: Critical Reward extraction or unfair reward access: any bug that lets a user access rewards they should not receive, enter or manipulate state before rewards are distributed, bypass eligibility or timing rules, claim more rewards than other users with the same entitlement, repeat/replay reward claims, or otherwise gain excess reward value from the protocol'] [Function: BorrowerOperations._moveTokensAndBNBfromAdjustment / activePool.sendBNB(borrower, collChange) / msg.sender vs _borrower] Can an unprivileged ATTACKER under PRECONDITIONS (in _adjustTrove, the MoveTokensAndBNBFromAdjustmentParam uses borrower: msg.sender for the USDS change but the trove being adjusted is _borrower) trigger CALL_SEQUENCE (StabilityPool calls moveBNBGainToTrove(_borrower, ...) -> _adjustTrove is called with _borrower != msg.sender -> in _moveTokensAndBNBfromAdjustment, if isCollIncrease=true, _activePoolAddColl sends ETH to ActivePool correctly -> but if isDebtIncrease=true, _withdrawUSDS mints USDS to param.borrower which is msg.sender (StabilityPool) not _borrower), violating INVARIANT (in moveBNBGainToTrove, USDSChange must be 0 and isDebtIncrease must be false; the assert in _adjustTrove enforces this; but if this assert can be bypassed, USDS could be minted to the wrong address), causing scoped impact: verify the assert(msg.sender == _borrower || (msg.sender == stabilityPoolAddress && collateral > 0 && USDSChange == 0)) correctly prevents any debt increase when called from StabilityPool; if bypassable, StabilityPool could trigger USDS minting to itself? Local proof idea: write a unit test attempting to call moveBNBGainToTrove with USDSChange > 0; assert it reverts."
]