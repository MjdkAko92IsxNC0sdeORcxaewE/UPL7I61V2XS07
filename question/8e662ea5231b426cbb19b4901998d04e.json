[
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: hashOperation / salt reuse → schedule same action multiple times] Can an attacker with PROPOSER_ROLE schedule the same SystemState parameter change multiple times using different salts under the precondition that _schedule only checks !isOperation(id) and id is derived from (target, value, data, predecessor, salt), by calling schedule(SystemState, 0, setMCR(min), bytes32(0), salt1, minDelay) and schedule(SystemState, 0, setMCR(min), bytes32(0), salt2, minDelay) with different salts, creating two independent pending operations for the same action, violating the invariant that a single parameter change should require a single timelock cycle, causing scoped impact: attacker queues multiple copies of MCR reduction; if one is cancelled by a guardian, the other still executes, bypassing the cancellation protection? Local proof idea: add Foundry unit test, schedule same setMCR call with two different salts, cancel one, execute the other, assert MCR is changed despite cancellation of the first.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setMCR + setRedemptionFeeFloor(0) sequential] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE combine MCR reduction with zero redemption fee to extract maximum BNB under the precondition that both changes are executed sequentially via TimeLock, by first executing setMCR(1000000000000000001) then setRedemptionFeeFloor(0), then opening a trove at 100.0001% ICR to borrow maximum USDS, then immediately redeeming that USDS for BNB with zero fee, violating the invariant that borrowing and redemption fees together prevent profitable round-trip extraction, causing scoped impact: attacker borrows USDS at near-100% ICR with zero borrowing fee and redeems for BNB with zero redemption fee, extracting BNB value from the protocol in a near-zero-cost round trip? Local proof idea: add Foundry integration test, set MCR to min and redemptionFeeFloor to 0 via TimeLock, open trove at 100.1% ICR, redeem USDS for BNB, assert net BNB extracted > BNB deposited minus dust.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: executeBatch / MCR+CCR+feeFloors all in one batch] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE atomically set all four extraction-enabling parameters in a single batch under the precondition that the batch ordering satisfies MCR/CCR constraints (setCCR first, then setMCR, then fee floors), by scheduling a batch with targets=[SystemState x4], datas=[setCCR(1100000000000000002), setMCR(1000000000000000001), setBorrowingFeeFloor(0), setRedemptionFeeFloor(0)], executing after the delay, violating the invariant that MCR, CCR, borrowingFeeFloor, and redemptionFeeFloor are independently protected and cannot all be minimized simultaneously, causing scoped impact: attacker atomically enables maximum USDS extraction at minimum collateral with zero fees in a single timelock cycle? Local proof idea: add Foundry integration test, execute the four-parameter batch, assert all four parameters are at minimum, then open trove at 100.1% ICR with zero fee and redeem with zero fee.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / _beforeCall predecessor=bytes32(0) bypass] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE bypass predecessor dependency checks by always using predecessor=bytes32(0) under the precondition that _beforeCall only requires predecessor == bytes32(0) || isOperationDone(predecessor), by scheduling all operations with predecessor=bytes32(0) regardless of intended sequencing, violating the invariant that predecessor dependencies enforce a required execution order for multi-step parameter changes, causing scoped impact: attacker executes MCR reduction without waiting for a required prior operation (e.g., a safety check operation) to complete, enabling out-of-order parameter changes that bypass intended governance sequencing? Local proof idea: add Foundry unit test, schedule two operations where op2 should depend on op1 but uses predecessor=bytes32(0), execute op2 before op1, assert op2 succeeds without op1 being done.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setUSDSGasCompensation(huge) cross-module → compositeDebt inflation DoS on existing troves] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE prevent existing trove owners from repaying their debt by increasing USDSGasCompensation to a value larger than existing trove debt via TimeLock under the precondition that _requireValidUSDSRepayment computes _currentDebt.sub(USDS_GAS_COMPENSATION) with SafeMath, by scheduling+executing setUSDSGasCompensation(existingTroveDebt + 1) via TimeLock, causing _requireValidUSDSRepayment to revert on underflow for all existing troves, violating the invariant that trove owners can always repay their debt to close their trove, causing scoped impact: existing trove owners cannot repay or close their troves, their collateral is trapped, and the attacker (as a liquidator) can liquidate those troves once their ICR drops below MCR, extracting gas compensation and BNB gains? Local proof idea: add Foundry integration test, open trove with 100e18 debt, set USDSGasCompensation to 101e18 via TimeLock, attempt repayUSDS, assert it reverts with SafeMath underflow.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setMinNetDebt cross-module → redemption partial cancellation bypass] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE manipulate partial redemption behavior by changing minNetDebt via TimeLock under the precondition that _redeemCollateralFromTrove cancels partial redemption if _getNetDebt(newDebt) < minNetDebt, by scheduling+executing setMinNetDebt(1) via TimeLock, then calling redeemCollateral() with a partial amount that would leave newDebt just above 1 wei, violating the invariant that minNetDebt prevents dust residual debt after partial redemption, causing scoped impact: attacker redeems almost all of a trove's debt leaving 1 wei residual, bypassing the partial redemption cancellation guard and extracting near-full BNB collateral value from the trove? Local proof idea: add Foundry integration test, set minNetDebt to 1 via TimeLock, redeem all but 1 wei of a trove's debt, assert redemption succeeds and attacker receives near-full BNB equivalent.",
  "[File: 'File Name: src/TimeLock.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: execute / SystemState.setMCR cross-module → redeemCollateral MCR filter bypass] Can an attacker with PROPOSER_ROLE and EXECUTOR_ROLE bypass the MCR filter in redeemCollateral by reducing MCR via TimeLock under the precondition that redeemCollateral skips troves with ICR < MCR (the while loop in TroveManager), by scheduling+executing setMCR(1000000000000000001) via TimeLock, then calling redeemCollateral() which now includes troves with ICR just above 100% in the redemption traversal, violating the invariant that only troves with ICR >= MCR (110%) are eligible for redemption, causing scoped impact: attacker redeems from troves that were previously excluded from redemption due to low ICR, extracting BNB from near-insolvent troves at face value? Local proof idea: add Foundry integration test, open trove at 105% ICR, reduce MCR to 1000000000000000001 via TimeLock, call redeemCollateral, assert the 105% ICR trove is now included in redemption traversal."
]