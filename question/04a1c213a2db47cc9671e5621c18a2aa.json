[
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged borrower who submits a valid Pyth price update with deviationPyth=0 and publishTimePyth exactly equal to block.timestamp under the precondition that the Pyth oracle accepts zero-confidence price updates trigger openTrove -> getOracleRate(bytes32('PYTH'), 0, block.timestamp) -> _triggerBorrowingFee with oracleRate=0, violating the invariant that every USDS mint must correspond to a fee no lower than the protocol-intended minimum oracle-adjusted rate, causing scoped impact: attacker mints USDS at the absolute minimum borrowing fee (BORROWING_FEE_FLOOR only, with zero oracle surcharge), extracting USDS value at below-intended cost? Local proof idea: add a unit test that calls openTrove with crafted priceFeedUpdateData yielding deviationPyth=0 and publishTimePyth=block.timestamp, assert USDSFee == troveManager.getBorrowingFee(USDSAmount, 0) and that this is strictly less than getBorrowingFee(USDSAmount, MAX_ORACLE_RATE_PERCENTAGE), confirming fee reduction.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged redeemer who submits a valid Pyth update with deviationPyth=0 and publishTimePyth=block.timestamp under the precondition that baseRate has decayed to near zero trigger redeemCollateral -> getOracleRate -> getRedemptionRate(oracleRate=0), violating the invariant that redemption fees must reflect oracle uncertainty to protect the protocol from cheap BNB extraction, causing scoped impact: attacker redeems USDS for BNB at the minimum possible redemption fee, extracting more BNB per USDS than the protocol intends when oracle uncertainty is present? Local proof idea: fuzz test redeemCollateral with varying deviationPyth and publishTimePyth values, assert that BNB received per USDS is strictly bounded by the expected fee floor and that zero-deviation inputs do not yield more BNB than a non-zero-deviation baseline.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged borrower under the precondition that the Pyth network publishes a price with publishTimePyth slightly in the future (e.g., publishTimePyth = block.timestamp + 1) trigger getOracleRate where absDelayTime = publishTimePyth - block.timestamp = 1, yielding oracleRate = deviationPyth + 1*DECIMAL_PRECISION/10000 = deviationPyth + 1e14, violating the invariant that future-timestamped prices should not reduce the oracle rate below the deviation-only baseline, causing scoped impact: attacker exploits the symmetric absDelayTime formula to treat a future-dated price as having minimal delay, obtaining a lower oracleRate than a stale past-dated price of equal age would produce? Local proof idea: unit test getOracleRate with publishTimePyth = block.timestamp + N for various N, compare returned oracleRate to getOracleRate with publishTimePyth = block.timestamp - N, assert symmetry does not create a lower-fee path for future timestamps.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged borrower under the precondition that a non-PYTH oracle key (e.g., Chainlink fallback) is active trigger openTrove -> getOracleRate(oracleKey != bytes32('PYTH'), *, *) -> oracleRate = MAX_ORACLE_RATE_PERCENTAGE always, violating the invariant that the oracle rate should reflect actual price freshness and deviation rather than a fixed maximum, causing scoped impact: when Chainlink is active, all borrowers always pay the maximum oracle surcharge regardless of actual price quality, but more critically, if an attacker can force a switch from PYTH to Chainlink path for a victim while themselves using PYTH with zero deviation, the attacker borrows at lower cost while victims pay maximum? Local proof idea: unit test getOracleRate with oracleKey=bytes32(0) and oracleKey=bytes32('PYTH') with identical deviationPyth/publishTimePyth, assert the non-PYTH path always returns MAX_ORACLE_RATE_PERCENTAGE and document the fee differential.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate / _triggerBorrowingFee] Can an unprivileged borrower under the precondition that baseRate=0 (fully decayed) and oracleRate=0 (zero deviation, current timestamp) trigger openTrove -> getBorrowingFee(USDSAmount, 0) where total fee = max(0 + 0, BORROWING_FEE_FLOOR) = BORROWING_FEE_FLOOR, violating the invariant that the oracle rate component must add a non-zero surcharge when Pyth price data carries uncertainty, causing scoped impact: attacker mints maximum USDS at the absolute protocol fee floor with no oracle uncertainty premium, effectively borrowing at the cheapest possible rate and extracting USDS value below the intended oracle-adjusted cost? Local proof idea: invariant test asserting that for any valid Pyth update with non-zero confidence interval, oracleRate > 0, and that the protocol enforces a minimum oracle surcharge above BORROWING_FEE_FLOOR.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged attacker under the precondition that absDelayTime is large enough that absDelayTime.mul(DECIMAL_PRECISION) overflows uint256 trigger getOracleRate with publishTimePyth far in the future (publishTimePyth >> block.timestamp + type(uint256).max/DECIMAL_PRECISION) causing SafeMath.mul to revert, violating the invariant that getOracleRate must not revert on any valid uint input pair, causing scoped impact: if the Pyth oracle can ever return a publishTimePyth that causes overflow before the MAX cap is applied, all borrowing and redemption operations revert, and if the attacker can selectively trigger this for specific blocks, they can time extraction operations to windows where the revert is avoided? Local proof idea: fuzz test getOracleRate with publishTimePyth in range [block.timestamp, type(uint256).max], identify the threshold where SafeMath.mul reverts, and confirm the cap check at line 25 is unreachable for overflow inputs.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged borrower under the precondition that deviationPyth alone already equals or exceeds MAX_ORACLE_RATE_PERCENTAGE trigger getOracleRate where oracleRate is immediately capped before adding the delay component, violating the invariant that both deviation and delay should independently contribute to the oracle rate surcharge, causing scoped impact: when deviationPyth >= MAX_ORACLE_RATE_PERCENTAGE, the delay component is silently discarded and oracleRate = MAX_ORACLE_RATE_PERCENTAGE regardless of how stale the price is, meaning a borrower with a high-deviation but current price pays the same fee as one with a high-deviation and very stale price, creating a fee equivalence that may undercharge for combined risk? Local proof idea: unit test getOracleRate with deviationPyth=MAX_ORACLE_RATE_PERCENTAGE and publishTimePyth=block.timestamp-3600, assert returned value equals MAX_ORACLE_RATE_PERCENTAGE and document that the stale-price surcharge is lost.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate / openTrove] Can an unprivileged borrower under the precondition that they can select among multiple valid Pyth price updates (e.g., one from 1 second ago with deviationPyth=0 vs one from 60 seconds ago with deviationPyth=1e14) trigger openTrove with the update that minimizes oracleRate, violating the invariant that the protocol should use the most recent price update and not allow fee shopping across multiple valid updates, causing scoped impact: attacker cherry-picks the Pyth update that yields the lowest oracleRate and thus the lowest borrowing fee, extracting USDS at below-intended cost by exploiting the caller-controlled priceFeedUpdateData parameter? Local proof idea: unit test openTrove with two different priceFeedUpdateData arrays yielding different oracleRates, assert that the protocol enforces use of the freshest/highest-deviation update rather than allowing the caller to choose the cheapest one.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged borrower under the precondition that absDelayTime * DECIMAL_PRECISION / 10000 rounds down to zero for absDelayTime < 10000/DECIMAL_PRECISION (i.e., absDelayTime = 0 seconds) trigger getOracleRate where the delay component contributes exactly 0 to oracleRate, violating the invariant that any non-zero price staleness should produce a non-zero delay surcharge, causing scoped impact: for publishTimePyth = block.timestamp (zero delay), the delay term is 0 and oracleRate = deviationPyth only, which is correct, but for publishTimePyth = block.timestamp - 1 (1 second stale), the delay term is 1e18/10000 = 1e14 which is non-zero — confirm there is no rounding-to-zero path that silently drops the delay surcharge for small but non-zero staleness? Local proof idea: fuzz test getOracleRate with absDelayTime in [0, 100], assert delay component = absDelayTime * DECIMAL_PRECISION / 10000 with no unexpected zero truncation.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate / redeemCollateral] Can an unprivileged redeemer under the precondition that they submit a Pyth update with publishTimePyth = block.timestamp and deviationPyth = 0 trigger redeemCollateral -> getOracleRate -> getRedemptionRate(oracleRate=0) -> redemption fee = baseRate + 0, violating the invariant that redemption fees must include an oracle uncertainty component when price data is uncertain, causing scoped impact: attacker redeems large USDS amounts for BNB at the minimum fee (baseRate only), extracting more BNB collateral than the protocol intends when oracle quality is poor, effectively draining BNB from the ActivePool at a discount? Local proof idea: differential test comparing BNB received from redeemCollateral with oracleRate=0 vs oracleRate=MAX_ORACLE_RATE_PERCENTAGE for the same USDS input, assert the fee difference represents extractable BNB value.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged attacker under the precondition that deviationPyth is a very large uint (close to type(uint256).max) trigger getOracleRate where deviationPyth.add(absDelayTime.mul(DECIMAL_PRECISION).div(10000)) overflows SafeMath before the cap check, causing a revert instead of capping at MAX_ORACLE_RATE_PERCENTAGE, violating the invariant that getOracleRate must return a valid capped value for any uint input, causing scoped impact: if PriceFeed can ever return a deviationPyth near uint256 max (e.g., due to Pyth confidence interval encoding), all borrowing/redemption operations revert, and an attacker who can predict this window can front-run to extract value before the DoS? Local proof idea: fuzz test getOracleRate with deviationPyth in [MAX_ORACLE_RATE_PERCENTAGE, type(uint256).max] and absDelayTime > 0, identify inputs where SafeMath.add reverts instead of capping.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate / adjustTrove] Can an unprivileged borrower under the precondition that they have an existing trove and can submit a Pyth update with oracleRate=0 trigger withdrawUSDS -> _adjustTrove -> getOracleRate(0) -> _triggerBorrowingFee with minimum fee, violating the invariant that debt increases must always incur an oracle-adjusted fee reflecting current price uncertainty, causing scoped impact: attacker repeatedly calls withdrawUSDS with zero-deviation Pyth updates to extract additional USDS at the minimum fee, accumulating USDS debt at below-intended cost across multiple adjustments? Local proof idea: state-transition test calling withdrawUSDS N times with oracleRate=0 vs oracleRate=MAX_ORACLE_RATE_PERCENTAGE, assert cumulative fee difference and total USDS minted.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged attacker under the precondition that the Pyth oracle is temporarily unavailable and the system falls back to a non-PYTH oracle key trigger getOracleRate(oracleKey != 'PYTH', *, *) -> oracleRate = MAX_ORACLE_RATE_PERCENTAGE, and then immediately after Pyth becomes available submit a Pyth update with deviationPyth=0 and publishTimePyth=block.timestamp to get oracleRate=0, violating the invariant that oracle rate transitions between fallback and primary oracle should not create exploitable fee discontinuities, causing scoped impact: attacker times borrowing/redemption operations to the exact block where Pyth resumes with a fresh zero-deviation price, extracting value at minimum fee after a period of maximum-fee enforcement? Local proof idea: integration test simulating oracle fallback -> recovery transition, assert oracleRate continuity and that the fee jump from MAX to 0 cannot be exploited in a single block.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate / getBorrowingFee] Can an unprivileged borrower under the precondition that oracleRate=0 and baseRate=0 trigger openTrove where getBorrowingFee returns exactly BORROWING_FEE_FLOOR * USDSAmount / DECIMAL_PRECISION, violating the invariant that the total borrowing fee must be strictly greater than BORROWING_FEE_FLOOR when oracle uncertainty exists, causing scoped impact: attacker opens a maximum-size trove at the absolute minimum fee, minting the maximum possible USDS for the minimum fee cost, with the fee savings representing direct value extraction relative to the intended oracle-adjusted fee schedule? Local proof idea: unit test asserting getBorrowingFee(largeAmount, 0) == BORROWING_FEE_FLOOR * largeAmount / DECIMAL_PRECISION and computing the USDS fee savings vs getBorrowingFee(largeAmount, MAX_ORACLE_RATE_PERCENTAGE).",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged attacker under the precondition that publishTimePyth is set to exactly block.timestamp - 1 (one second stale) trigger getOracleRate where absDelayTime=1 and delay component = 1e18/10000 = 1e14 (0.00001% in 1e18 precision), violating the invariant that the delay surcharge must be economically meaningful relative to the deviation component, causing scoped impact: for a 1-second-old price with deviationPyth=0, oracleRate = 1e14 which is 0.00001% — confirm this is the intended minimum non-zero oracle rate and that it does not allow near-zero-fee borrowing that the protocol did not intend? Local proof idea: unit test getOracleRate with absDelayTime=1 and deviationPyth=0, assert oracleRate=1e14, then compute the resulting borrowing fee on a 1M USDS borrow and verify it is economically significant.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate / redeemCollateral] Can an unprivileged redeemer under the precondition that they hold a large USDS balance and can submit a Pyth update with publishTimePyth=block.timestamp and deviationPyth=0 trigger redeemCollateral in a loop across multiple blocks, each time submitting a fresh zero-deviation Pyth update, violating the invariant that repeated redemptions must not allow systematic BNB extraction at below-intended fees, causing scoped impact: attacker drains BNB from the ActivePool across multiple redemption transactions, each at minimum oracleRate=0, accumulating BNB extraction savings that compound with each redemption? Local proof idea: multi-block simulation test calling redeemCollateral 10 times with oracleRate=0, assert total BNB extracted vs total USDS burned matches the expected fee-adjusted rate and that no excess BNB is received.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged attacker under the precondition that they can observe the mempool and see a pending Pyth price update with high deviationPyth trigger a front-run where they submit their own borrowing transaction with the current low-deviation Pyth state before the high-deviation update is processed, violating the invariant that borrowing fees must reflect the oracle uncertainty at the time of the transaction, causing scoped impact: attacker front-runs a high-deviation Pyth update to borrow USDS at the current low oracleRate, extracting USDS at a fee that does not reflect the imminent oracle uncertainty that would have increased their fee? Local proof idea: two-transaction test where tx1 borrows with oracleRate=0 and tx2 updates Pyth to high deviation, assert that the protocol cannot prevent this ordering and quantify the fee savings.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayth, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged attacker under the precondition that the Pyth oracle publishTimePyth is in the future (publishTimePyth > block.timestamp) by a small amount (e.g., 1 second) trigger getOracleRate where absDelayTime = publishTimePyth - block.timestamp = 1, yielding the same oracleRate as a price that is 1 second old, violating the invariant that future-timestamped prices should be treated as maximally uncertain (not minimally stale), causing scoped impact: attacker submits a Pyth update with a slightly future timestamp to obtain the same low oracleRate as a perfectly current price, bypassing the intended staleness penalty and borrowing/redeeming at minimum fee? Local proof idea: unit test getOracleRate with publishTimePyth = block.timestamp + 1 vs publishTimePyth = block.timestamp - 1, assert both return the same oracleRate and document that future timestamps are not penalized.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate / openTrove] Can an unprivileged borrower under the precondition that they open multiple troves across different accounts, each time submitting a fresh Pyth update with oracleRate=0, trigger a sequence of openTrove calls that collectively mint a large amount of USDS at minimum fees, violating the invariant that the aggregate borrowing fee collected by the protocol must reflect oracle uncertainty across all minting events, causing scoped impact: attacker deploys multiple borrower contracts, each opening a trove with oracleRate=0, collectively extracting USDS at below-intended cost and accumulating the fee savings as profit? Local proof idea: multi-account fuzz test opening N troves with oracleRate=0, assert total fees collected equals N * BORROWING_FEE_FLOOR * USDSAmount / DECIMAL_PRECISION and compute savings vs intended oracle-adjusted fees.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged attacker under the precondition that MAX_ORACLE_RATE_PERCENTAGE = 25 * DECIMAL_PRECISION / 10000 = 2500000000000000 (2.5e15) is computed with integer division trigger a scenario where the intended 0.25% cap is actually 0.25% - epsilon due to integer truncation, violating the invariant that MAX_ORACLE_RATE_PERCENTAGE must equal exactly 0.25% in 1e18 fixed-point, causing scoped impact: if 25 * 1e18 / 10000 truncates (it does not since 25e18 is divisible by 10000), confirm no truncation error exists, but if a future parameter change introduces a non-divisible constant, the cap could be lower than intended, allowing higher-than-capped oracle rates? Local proof idea: unit test asserting MAX_ORACLE_RATE_PERCENTAGE * 10000 == 25 * DECIMAL_PRECISION with no remainder, and fuzz test all candidate MAX values for truncation.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate / _triggerBorrowingFee] Can an unprivileged borrower under the precondition that oracleRate=0 and the sableStaking.increaseF_USDS(USDSFee) call receives a fee of exactly BORROWING_FEE_FLOOR * USDSAmount / DECIMAL_PRECISION trigger openTrove, violating the invariant that the SABLE staking contract must receive the full oracle-adjusted fee (not just the floor fee), causing scoped impact: SABLE stakers receive less fee revenue than intended when oracleRate=0, representing a reward extraction from stakers — the attacker borrows cheaply while stakers are underpaid relative to the oracle-adjusted fee schedule? Local proof idea: unit test asserting increaseF_USDS receives BORROWING_FEE_FLOOR * amount vs MAX_ORACLE_RATE_PERCENTAGE * amount, compute the staker reward shortfall per borrow.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged attacker under the precondition that they can submit priceFeedUpdateData with publishTimePyth = 0 (Unix epoch) trigger getOracleRate where absDelayTime = block.timestamp - 0 = block.timestamp (approximately 1.7e9 seconds), and absDelayTime.mul(DECIMAL_PRECISION) = 1.7e9 * 1e18 = 1.7e27 which exceeds uint256 max / 10000 but not uint256 max itself, causing SafeMath.mul to succeed but the result before div(10000) to be 1.7e27, which after div(10000) = 1.7e23 >> MAX_ORACLE_RATE_PERCENTAGE, so oracleRate is capped at MAX — but does SafeMath.mul(1.7e9, 1e18) overflow? 1.7e9 * 1e18 = 1.7e27 < 1.15e77 (uint256 max), so no overflow — confirm the cap correctly handles this extreme staleness case and that no revert occurs? Local proof idea: unit test getOracleRate with publishTimePyth=0 and deviationPyth=0, assert oracleRate == MAX_ORACLE_RATE_PERCENTAGE without revert.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged attacker under the precondition that publishTimePyth is set to type(uint256).max trigger getOracleRate where absDelayTime = type(uint256).max - block.timestamp (a huge number), and absDelayTime.mul(DECIMAL_PRECISION) overflows SafeMath causing a revert, violating the invariant that getOracleRate must not revert for any uint256 input, causing scoped impact: if PriceFeed ever returns publishTimePyth = type(uint256).max (e.g., due to a Pyth encoding bug or malicious update), all borrowing and redemption operations revert permanently until a new price is fetched, and an attacker who can trigger this state can block all protocol operations while themselves having pre-positioned funds? Local proof idea: unit test getOracleRate with publishTimePyth = type(uint256).max, assert it either returns MAX_ORACLE_RATE_PERCENTAGE or reverts, and if it reverts, document the DoS vector.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate / redeemCollateral] Can an unprivileged redeemer under the precondition that they submit a Pyth update with deviationPyth=0 and publishTimePyth=block.timestamp trigger redeemCollateral where the redemption fee = baseRate + oracleRate = baseRate + 0, and if baseRate has also decayed to 0, the total redemption fee = 0 + 0 = 0 (before any floor), violating the invariant that redemption fees must always be positive to prevent zero-cost BNB extraction, causing scoped impact: attacker redeems USDS for BNB at zero oracle surcharge when both baseRate and oracleRate are zero, extracting BNB at 1:1 USDS value with no fee, effectively arbitraging any USDS discount for free? Local proof idea: unit test redeemCollateral with baseRate=0 and oracleRate=0, assert redemption fee > 0 due to a floor, and if no floor exists, document the zero-fee redemption extraction path.",
  "[File: 'File Name: src/OracleRateCalculation.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: getOracleRate] Can an unprivileged attacker under the precondition that they can observe the exact block.timestamp of the next block trigger getOracleRate by submitting a Pyth update with publishTimePyth = next_block.timestamp (predicted), so that when the transaction executes, absDelayTime = 0 and oracleRate = deviationPyth only, violating the invariant that the delay component must reflect actual price age at execution time, causing scoped impact: attacker uses block timestamp prediction (common on BNB Chain with ~3s block times) to submit a Pyth update timed to the execution block, eliminating the delay surcharge and borrowing/redeeming at minimum fee? Local proof idea: simulation test with block.timestamp prediction, assert that a Pyth update submitted in block N with publishTimePyth = block N timestamp yields absDelayTime=0 and oracleRate=deviationPyth."
]