[
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.unstake() + sableToken.transfer failure] Can an unprivileged attacker under the precondition that sableToken.transfer(msg.sender, SABLEGain) fails (e.g., SABLE token is paused or blacklists the recipient), cause the entire unstake() transaction to revert after the snapshot has been updated and LP tokens have been transferred (if sableToken.transfer is called after sableLPToken.transfer), permanently locking the user's ability to unstake if the SABLE token remains non-transferable, violating the invariant that stakers can always withdraw their LP tokens, causing scoped impact: LP tokens permanently locked? Local proof idea: deploy a mock SABLE token that reverts on transfer; call unstake(); assert LP tokens not returned and transaction reverts.",
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.unstake() state ordering: snapshot before LP transfer] Can an unprivileged attacker under the precondition that _updateUserSnapshots is called before sableLPToken.transfer in unstake(), and sableLPToken.transfer triggers a callback (ERC777 sent hook) that reenters unstake(), cause the reentrant unstake() to see the updated snapshot (zero pending gains) and the already-reduced stake, but still attempt to transfer LP tokens (SableLPToWithdraw = min(_sableLPAmount, newStake)), violating the invariant that LP tokens are transferred at most once per unstake call, causing scoped impact: double LP token withdrawal? Local proof idea: deploy ERC777 LP token with sent hook that reenters unstake(); assert LP tokens received == 2x original stake.",
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.stake() + unstake() cross-user reward dilution] Can an unprivileged attacker under the precondition that they stake a very large amount of LP tokens just before issueSABLE() is triggered, diluting other stakers' SABLEGainPerSABLEStaked (since totalSableLPStaked increases), then immediately unstake after claiming their proportional share, violating the invariant that stakers cannot extract more SABLE than their time-weighted proportional share, causing scoped impact: attacker extracts SABLE rewards that should belong to long-term stakers? Local proof idea: stake large amount; trigger issueSABLE(); unstake; assert attacker's SABLEGain > proportional share based on time staked.",
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.increaseF_SABLE() + F_SABLE precision loss accumulation] Can an unprivileged attacker under the precondition that many small issueSABLE() calls are made (each with small _SABLEGain and large totalSableLPStaked), causing repeated truncation in SABLEGainPerSABLEStaked = _SABLEGain.mul(DECIMAL_PRECISION).div(totalSableLPStaked), accumulate a significant gap between total SABLE transferred to SableStakingV2 and total SABLE claimable via F_SABLE, violating the invariant that sableToken.balanceOf(SableStakingV2) >= sum of all pending SABLEGains, causing scoped impact: SABLE accumulates in SableStakingV2 as unclaimable dust, or conversely, if the gap is negative, stakers cannot claim their full entitlement? Local proof idea: run 1000 small issueSABLE() calls; assert balanceOf(SableStakingV2) - sum(pendingSABLEGain) >= 0.",
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.stake() + issueSABLE() + F_SABLE snapshot race] Can an unprivileged attacker under the precondition that two users call stake() in the same block (same transaction ordering), and issueSABLE() is called once per stake() call, cause the second staker's issueSABLE() call to issue 0 SABLE (timeSinceLastIssue = 0 since lastIssuanceTime was updated by the first call), but both stakers get their snapshots set to the same F_SABLE value, and verify whether this creates any reward extraction opportunity? Local proof idea: two users stake in same block; advance time; both unstake; assert total SABLEGain == latestRewardPerSec * elapsed_time.",
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.unstake() + BNB gain send to contract with no receive()] Can an unprivileged attacker under the precondition that a contract staker has accumulated BNB gains but its receive() function reverts (or it has no receive()), cause _sendBNBGainToUser to fail, reverting the entire unstake() transaction including the LP token transfer, permanently locking the staker's LP tokens if the BNB gain is non-zero and the contract cannot receive BNB, violating the invariant that stakers can always withdraw their LP tokens, causing scoped impact: LP tokens permanently locked in SableStakingV2? Local proof idea: deploy a contract staker with no receive(); accumulate BNB gain; call unstake(); assert revert and LP tokens not returned.",
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.stake() + unstake() with zero SABLEGain but non-zero sableToken.transfer] Can an unprivileged attacker under the precondition that SABLEGain = 0 (due to rounding or zero F_SABLE increase), cause sableToken.transfer(msg.sender, 0) to be called, and verify whether the SABLE token's transfer(address, 0) reverts (some ERC20 implementations revert on zero transfer), causing unstake() to revert even when the user has no SABLE gain, violating the invariant that users can always unstake when they have a valid stake? Local proof idea: deploy a mock SABLE token that reverts on transfer(addr, 0); call unstake() with zero SABLEGain; assert revert.",
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.stake() + unstake() with zero USDSGain but non-zero usdsToken.transfer] Can an unprivileged attacker under the precondition that USDSGain = 0 (no USDS fees have been deposited), cause usdsToken.transfer(msg.sender, 0) to be called in unstake(), and verify whether USDSToken reverts on zero transfer (checking USDSToken.sol implementation), causing unstake() to always revert for users with zero USDS gain, violating the invariant that users can always unstake? Local proof idea: deploy system with no USDS fees; stake; unstake; assert whether USDSToken.transfer(addr, 0) reverts.",
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.stake() currentStake=0 branch skips gain distribution] Can an unprivileged attacker under the precondition that they have a pre-existing stake (currentStake > 0) and accumulated gains, call stake() which correctly distributes gains (if (currentStake != 0) branch), but verify whether the snapshot update (_updateUserSnapshots) happens before or after the gain computation, and whether a front-running attack (another user calls increaseF_BNB between the gain computation and snapshot update) could cause the attacker to miss gains or double-claim, violating the invariant that all gains accrued before stake() are distributed? Local proof idea: front-run stake() with increaseF_BNB(); assert attacker's BNB gain includes the front-run fee.",
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.increaseF_SABLE() caller check + sableRewarderAddress] Can an unprivileged attacker under the precondition that sableRewarderAddress is set to a contract they control (e.g., if setAddresses is callable again or if there's a reinitialization bug), call increaseF_SABLE(type(uint256).max) to inflate F_SABLE to near-maximum, causing all stakers' pending SABLE gains to overflow in _getPendingSABLEGain() (stakes[user].mul(F_SABLE.sub(F_SABLE_Snapshot)) overflows SafeMath), permanently bricking unstake() for all stakers, violating the invariant that stakers can always withdraw, causing scoped impact: all LP tokens permanently locked? Local proof idea: call increaseF_SABLE(type(uint256).max) from sableRewarderAddress; call unstake(); assert SafeMath overflow revert.",
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.setAddresses() + initialized flag] Can an unprivileged attacker under the precondition that setAddresses() is callable by the owner (onlyOwner) and there is no initialized flag preventing re-initialization (unlike setParams in SableRewarder which has require(!initialized)), cause the owner to re-call setAddresses() with a malicious sableRewarderAddress, redirecting increaseF_SABLE calls to an attacker-controlled contract that manipulates F_SABLE, violating the invariant that sableRewarderAddress is immutable after deployment, causing scoped impact: F_SABLE manipulation enabling reward over-extraction? Local proof idea: call setAddresses() twice with different sableRewarderAddress; assert second call succeeds and sableRewarderAddress is updated.",
  "[File: 'src/SableRewarder.sol -> Scope: Critical Fund extraction or protocol value drain'] [Function: SableStakingV2.setSableLPAddress() + initialized flag] Can an unprivileged attacker under the precondition that setSableLPAddress() is callable by the owner and sets initialized=true, and the owner calls setSableLPAddress() a second time with a malicious LP token address (since there's no require(!initialized) check in setSableLPAddress), cause stake() to pull LP tokens from the malicious token (which could mint tokens freely), allowing the attacker to stake without providing real LP tokens, violating the invariant that stakes[user] corresponds to real LP tokens locked in SableStakingV2, causing scoped impact: attacker stakes with worthless tokens and claims BNB/USDS/SABLE rewards? Local proof idea: call setSableLPAddress() twice; stake with the new mock LP token; unstake and claim rewards; assert reward extraction without real LP deposit."
]