[
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: permit()] Can an unprivileged ATTACKER holding a valid SABLE permit signature submit a malleable variant (flipped s to secp256k1 upper half, toggled v) under PRECONDITIONS where the original permit has not yet been mined, causing the original permit transaction to revert (nonce consumed) while the attacker's transferFrom drains the approved amount, violating INVARIANT that a signed permit must not enable unauthorized token movement beyond the signer's intent, causing scoped impact: attacker extracts SABLE tokens from victim's balance via front-run permit replay with malleable (v, r, s)? Local proof idea: add unit test constructing two valid (v,r,s) pairs for the same digest using secp256k1 upper-s malleability, submit malleable variant first, assert original reverts and attacker's transferFrom succeeds draining victim SABLE.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: permit()] Can an unprivileged ATTACKER pass owner=address(0) and a degenerate (v=27, r=0, s=0) signature under PRECONDITIONS where ecrecover returns address(0) for invalid inputs, causing permit() to satisfy require(recoveredAddress == owner) when both are zero, then _approve(address(0), spender, amount) to revert, but intermediate state or gas behavior leaks value, violating INVARIANT that permit must reject all signatures not produced by the stated owner, causing scoped impact: unauthorized allowance set or state corruption enabling downstream SABLE extraction? Local proof idea: fuzz test calling permit(address(0), attacker, MAX_UINT, MAX_UINT, 27, 0, 0) and assert it always reverts without setting any allowance.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: permit()] Can an unprivileged ATTACKER use a permit signature created on a forked chain (same chainId, same contract address) under PRECONDITIONS where _CACHED_DOMAIN_SEPARATOR was cached at deployment and chainId has not changed, causing the cached domain separator to be reused across a chain fork, violating INVARIANT that a permit signature must be bound to exactly one chain and cannot be replayed on a fork, causing scoped impact: attacker replays victim's permit on a forked BNB chain to drain SABLE allowance and extract tokens? Local proof idea: differential test deploying SABLEToken on two forked environments with identical chainId, assert permit signed on fork A is accepted on fork B.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: transferFrom() / _requireValidRecipient()] Can an unprivileged ATTACKER who is the sableRewarderAddress call transferFrom(victim, sableStakingAddress, amount) under PRECONDITIONS where victim has approved sableRewarderAddress for any amount, causing _requireValidRecipient to skip the sableStakingAddress block because msg.sender == sableRewarderAddress, violating INVARIANT that only authorized protocol flows may move SABLE to the staking contract and victim's approval must not enable unauthorized redirection, causing scoped impact: attacker (as sableRewarder) drains victim's SABLE balance into the staking contract without victim's intent to stake? Local proof idea: unit test where sableRewarder calls transferFrom(victim, sableStakingAddress, victim_balance) after victim approves sableRewarder, assert victim balance decreases and staking contract balance increases without victim calling stake().",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: constructor() / _mint()] Can an unprivileged ATTACKER observe that _mint() in SABLEToken has no hard supply cap check under PRECONDITIONS where the constructor is called with _mintAmount exceeding 100e24 (100 million * 1e18), causing _totalSupply to exceed the documented hard cap of 100 million SABLE with no revert, violating INVARIANT that total SABLE supply must never exceed 100_000_000e18, causing scoped impact: excess SABLE minted at deployment inflates supply beyond cap, enabling attacker holding vault tokens to extract more protocol value than the cap allows? Local proof idea: unit test deploying SABLEToken with _mintAmount = 200_000_000e18 and asserting totalSupply() == 200_000_000e18 (no revert), proving cap is unenforced.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: permit() + transferFrom()] Can an unprivileged ATTACKER use permit() to set an allowance for themselves as spender under PRECONDITIONS where the victim signed a permit with deadline=uint(-1) (effectively infinite), then call transferFrom() repeatedly up to the approved amount, violating INVARIANT that permit-based allowances must be bounded by the signer's intent and not enable unbounded future extraction, causing scoped impact: attacker extracts victim's entire SABLE balance at any future time using a single permit signature? Local proof idea: fuzz test with random future timestamps calling transferFrom after permit with max deadline, assert cumulative extracted amount never exceeds permitted amount.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: transfer() / _requireValidRecipient()] Can an unprivileged ATTACKER call transfer(sableStakingAddress, amount) under PRECONDITIONS where msg.sender is any address other than sableRewarderAddress, causing _requireValidRecipient to revert, but then call transfer(sableStakingAddress, amount) as sableRewarderAddress (if sableRewarder is a contract the attacker controls or can call), violating INVARIANT that SABLE must not be sent directly to the staking contract except through authorized protocol flows, causing scoped impact: SABLE tokens locked in staking contract without corresponding stake accounting, creating phantom staking balance? Local proof idea: unit test calling transfer(sableStakingAddress, amount) from sableRewarderAddress, assert staking contract SABLE balance increases but stakes[] mapping is unchanged.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: CommunityIssuance.issueSABLE() + sendSABLE()] Can an unprivileged ATTACKER (as StabilityPool depositor) trigger repeated provideToSP/withdrawFromSP cycles under PRECONDITIONS where latestRewardPerSec is nonzero and CommunityIssuance SABLE balance is 5409915403529186761416 (live value), causing issueSABLE() to accumulate totalSABLEIssued beyond the actual SABLE balance held by CommunityIssuance, then sendSABLE() to transfer more SABLE than CommunityIssuance holds, violating INVARIANT that totalSABLEIssued must never exceed CommunityIssuance.balanceSABLE(), causing scoped impact: last depositor's SABLE reward claim reverts (DoS) or early claimants extract more than their proportional share? Local proof idea: invariant test asserting CommunityIssuance.sableToken.balanceOf(communityIssuance) >= sum of all pending SABLE rewards across all depositors after N issuance cycles.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SableStakingV2.stake() + unstake()] Can an unprivileged ATTACKER stake a minimal SABLE_LP amount under PRECONDITIONS where F_SABLE has accumulated (totalSableLPStaked was nonzero before attacker's stake), causing _updateUserSnapshots to record current F_SABLE before the attacker's stake is added, then immediately unstake to claim SABLEGain = stakes[attacker] * (F_SABLE - F_SABLE_Snapshot) / DECIMAL_PRECISION = 0 (since snapshot equals current F_SABLE), violating INVARIANT that a staker must not receive SABLE gains for periods before their stake existed, causing scoped impact: attacker extracts SABLE rewards by timing stake entry to capture accumulated F_SABLE delta? Local proof idea: unit test where attacker stakes after F_SABLE increases, assert getPendingSABLEGain(attacker) == 0 immediately after stake.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SableStakingV2.increaseF_SABLE() + stake()] Can an unprivileged ATTACKER observe that when totalSableLPStaked == 0, increaseF_SABLE() sets SABLEGainPerSABLEStaked = 0 and F_SABLE is not updated, under PRECONDITIONS where all stakers have unstaked and SableRewarder calls increaseF_SABLE with a nonzero _SABLEGain, causing the SABLE gain to be silently discarded (not added to F_SABLE), violating INVARIANT that all issued SABLE gains must be accounted for in F_SABLE or returned to the protocol, causing scoped impact: SABLE tokens issued by SableRewarder are permanently lost (not distributed to any staker), reducing total reward pool? Local proof idea: unit test calling increaseF_SABLE(1e18) when totalSableLPStaked == 0, assert F_SABLE unchanged and SABLE balance of staking contract unchanged.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SableStakingV2.unstake() + _sendBNBGainToUser()] Can an unprivileged ATTACKER deploy a receiver contract that reenters unstake() during the BNB transfer in _sendBNBGainToUser() under PRECONDITIONS where the attacker has a nonzero stake and BNBGain, causing a second unstake() call to execute before the first completes, violating INVARIANT that BNBGain must be claimed at most once per unstake and stakes must be updated before BNB is sent, causing scoped impact: attacker double-claims BNB gain from SableStakingV2? Local proof idea: unit test with malicious receiver contract reentering unstake(0) in receive(), assert total BNB received by attacker equals exactly one BNBGain and not double.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SableStakingV2.stake() + _sendBNBGainToUser()] Can an unprivileged ATTACKER deploy a receiver contract that reenters stake() during the BNB transfer in _sendBNBGainToUser() under PRECONDITIONS where the attacker has a pre-existing stake with accumulated BNBGain and the sableLPToken is a standard ERC20, causing the reentrant stake() to see the already-updated snapshot but the old stake value before the LP transferFrom, violating INVARIANT that BNBGain must be paid exactly once and stake accounting must be consistent across reentrant calls, causing scoped impact: attacker extracts extra BNB from SableStakingV2 via reentrant stake()? Local proof idea: unit test with malicious receiver reentering stake(1) in receive() during BNBGain send, assert total BNB extracted equals exactly one BNBGain.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SableStakingV2._getPendingSABLEGain() / _getPendingBNBGain() / _getPendingUSDSGain()] Can an unprivileged ATTACKER with a large stake observe that pending gain calculations use integer division stakes[user].mul(F_X.sub(F_X_Snapshot)).div(DECIMAL_PRECISION) under PRECONDITIONS where F_X - F_X_Snapshot is very small relative to DECIMAL_PRECISION, causing truncation to zero for small stakes while large stakers extract disproportionately more than their share, violating INVARIANT that total distributed gains must not exceed total fees collected, causing scoped impact: precision truncation allows large stakers to extract more than their proportional SABLE/BNB/USDS reward share? Local proof idea: fuzz test with random stake sizes and F_X deltas, assert sum of all pending gains never exceeds total accumulated fees.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SableStakingV2.stake() ordering] Can an unprivileged ATTACKER call stake() under PRECONDITIONS where sableRewarder.issueSABLE() is called first (updating F_SABLE via increaseF_SABLE), then _updateUserSnapshots captures the new F_SABLE, then stakes[msg.sender] is increased, causing the attacker's snapshot to be set to the post-issuance F_SABLE before their stake is counted in totalSableLPStaked, violating INVARIANT that a staker's snapshot must reflect the F_SABLE at the moment their stake begins earning, causing scoped impact: attacker avoids paying into the current issuance period and extracts future rewards without contributing to the current epoch? Local proof idea: unit test calling stake() immediately after a large issueSABLE event, assert attacker's pending SABLE gain is zero for the just-issued amount.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: CommunityIssuance.issueSABLE()] Can an unprivileged ATTACKER (as StabilityPool) call issueSABLE() in the same block multiple times under PRECONDITIONS where block.timestamp == lastIssuanceTime (same block), causing timeSinceLastIssue = 0 and issuance = 0, but totalSABLEIssued is not updated, violating INVARIANT that issuance must be monotonically increasing and each call must correctly account for elapsed time, causing scoped impact: no direct extraction but confirms issuance is idempotent within a block, preventing double-issuance? Local proof idea: unit test calling issueSABLE() twice in the same block, assert totalSABLEIssued increases only by one period's worth.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: CommunityIssuance.issueSABLE() + latestRewardPerSec] Can an unprivileged ATTACKER observe that issueSABLE() computes issuance = latestRewardPerSec.mul(timeSinceLastIssue) with no cap against remaining SABLE balance under PRECONDITIONS where latestRewardPerSec is set high and a long time has elapsed, causing totalSABLEIssued to exceed CommunityIssuance's actual SABLE balance, then sendSABLE() to transfer more than available, violating INVARIANT that sendSABLE must never transfer more SABLE than CommunityIssuance holds, causing scoped impact: first claimants extract all SABLE while later depositors receive nothing? Local proof idea: invariant fuzz test asserting sableToken.balanceOf(communityIssuance) >= sum of all claimable SABLE across all SP depositors at any point in time.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SableStakingV2.unstake() + usdsToken.transfer()] Can an unprivileged ATTACKER call unstake() under PRECONDITIONS where SableStakingV2's USDS balance is less than the computed USDSGain (e.g., due to accounting drift or prior extraction), causing usdsToken.transfer(msg.sender, USDSGain) to revert, violating INVARIANT that SableStakingV2 must always hold sufficient USDS to cover all pending USDSGain for all stakers, causing scoped impact: staker cannot withdraw their entitled USDS gain (DoS) or if transfer succeeds, extracts more USDS than the contract holds? Local proof idea: invariant test asserting usdsToken.balanceOf(sableStaking) >= sum of all getPendingUSDSGain(staker) for all stakers.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SableStakingV2.unstake() + sableToken.transfer()] Can an unprivileged ATTACKER call unstake() under PRECONDITIONS where SableStakingV2's SABLE balance is less than the computed SABLEGain (e.g., CommunityIssuance issued SABLE to F_SABLE but never transferred tokens to staking contract), causing sableToken.transfer(msg.sender, SABLEGain) to revert, violating INVARIANT that SableStakingV2 must hold sufficient SABLE to cover all pending SABLEGain, causing scoped impact: staker cannot claim entitled SABLE rewards or accounting mismatch enables over-extraction? Local proof idea: invariant test asserting sableToken.balanceOf(sableStaking) >= sum of all getPendingSABLEGain(staker) for all stakers.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SableStakingV2.increaseF_BNB() / increaseF_USDS() / increaseF_SABLE()] Can an unprivileged ATTACKER observe that F_BNB, F_USDS, and F_SABLE accumulators can overflow uint256 under PRECONDITIONS where fees are added over a very long period with high fee rates and low totalSableLPStaked (amplifying per-unit values), causing F_X to wrap around to a small value, violating INVARIANT that F_X must be monotonically non-decreasing and pending gain calculations must not underflow via F_X.sub(F_X_Snapshot), causing scoped impact: attacker with old snapshot extracts inflated rewards after overflow wraps F_X below their snapshot? Local proof idea: fuzz test with extreme _BNBFee values and minimal totalSableLPStaked, assert F_BNB never overflows and pending gain never exceeds total fees deposited.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SableStakingV2.stake() with 1 wei LP] Can an unprivileged ATTACKER stake 1 wei of SABLE_LP under PRECONDITIONS where F_SABLE has a large accumulated value from prior issuance periods, causing _updateUserSnapshots to record current F_SABLE, then wait for more F_SABLE to accumulate, then unstake to claim SABLEGain = 1 * (F_SABLE_new - F_SABLE_old) / DECIMAL_PRECISION, violating INVARIANT that minimum stake must not enable disproportionate reward extraction relative to contribution, causing scoped impact: attacker with dust stake extracts SABLE rewards proportional to their tiny stake but at the expense of other stakers' share? Local proof idea: unit test staking 1 wei, advancing time, asserting SABLEGain is proportional to 1/totalSableLPStaked and does not exceed fair share.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SABLEToken.permit() + SableStakingV2.stake()] Can an unprivileged ATTACKER use permit() to set allowance for themselves on a victim's SABLE_LP tokens (if SABLE_LP also implements EIP-2612) and then call stake() on behalf of victim under PRECONDITIONS where victim has signed a permit for SABLE_LP, causing the attacker to stake victim's LP tokens and claim the resulting staking rewards, violating INVARIANT that staking must only occur with the staker's own tokens and explicit consent, causing scoped impact: attacker stakes victim's SABLE_LP and extracts BNB/USDS/SABLE rewards that belong to the victim? Local proof idea: unit test using permit on SABLE_LP to approve attacker, then attacker calls stake() pulling victim's LP, assert attacker receives all staking rewards.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SABLEToken.approve() + transferFrom()] Can an unprivileged ATTACKER exploit the standard ERC20 approve race condition under PRECONDITIONS where a victim calls approve(attacker, newAmount) to change an existing nonzero allowance, causing the attacker to front-run with transferFrom(victim, attacker, oldAmount) before the new approval, then call transferFrom(victim, attacker, newAmount) after, violating INVARIANT that a victim's total transferred amount must not exceed the maximum of old or new approved amount, causing scoped impact: attacker extracts oldAmount + newAmount of SABLE from victim? Local proof idea: unit test simulating front-run: approve(100), transferFrom(100), approve(50), transferFrom(50), assert total extracted is 150 not 50.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: CommunityIssuance.sendSABLE() + StabilityPool reward path] Can an unprivileged ATTACKER as a StabilityPool depositor trigger withdrawFromSP() under PRECONDITIONS where CommunityIssuance has issued more SABLE (via issueSABLE) than it holds in balance, causing sendSABLE(depositor, amount) to attempt transferring more than available, violating INVARIANT that every SABLE reward claim must be backed by actual SABLE tokens in CommunityIssuance, causing scoped impact: first withdrawers extract all available SABLE while later depositors receive zero despite having earned rewards? Local proof idea: invariant test with multiple depositors, assert each depositor's claimable SABLE is backed by CommunityIssuance balance and no depositor receives more than their proportional share.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SableStakingV2.stake() + unstake() multi-cycle] Can an unprivileged ATTACKER repeatedly stake and unstake in alternating transactions under PRECONDITIONS where F_BNB, F_USDS, and F_SABLE increase between each cycle, causing each unstake to claim accumulated gains while each stake resets the snapshot, violating INVARIANT that total gains extracted by one staker must not exceed their proportional share of total fees collected during their staking period, causing scoped impact: attacker extracts more cumulative BNB/USDS/SABLE than their stake entitles them to through rapid cycling? Local proof idea: fuzz test with N stake/unstake cycles, assert sum of all extracted gains equals total fees * attacker_stake_fraction.",
  "[File: 'File Name: src/SABLE/SABLEToken.sol -> Scope: Critical Fund extraction or protocol value drain: any bug that lets a user gain, mint, borrow, redeem, withdraw, claim, or extract more funds than intended from the protocol, including minting value from nothing, flashloan-assisted extraction, oracle manipulation extraction, broken accounting, unauthorized withdrawals, over-redemption, underpayment, insolvency creation, or any other path that increases attacker-controlled funds at protocol or user expense'] [Function: SABLEToken._requireValidRecipient() + CommunityIssuance.sendSABLE()] Can an unprivileged ATTACKER observe that CommunityIssuance.sendSABLE() calls sableToken.transfer(_account, _SABLEamount) under PRECONDITIONS where _account is the sableStakingAddress, causing _requireValidRecipient to revert (since CommunityIssuance is not sableRewarderAddress), violating INVARIANT that CommunityIssuance must be able to send SABLE to any valid recipient including staking, causing scoped impact: SABLE reward distribution is permanently blocked if StabilityPool tries to route rewards to staking contract? Local proof idea: unit test calling sendSABLE(sableStakingAddress, amount) from StabilityPool, assert it reverts due to _requireValidRecipient."
]